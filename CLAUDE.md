# jr — Jira CLI

A Rust CLI tool for automating Jira Cloud workflows. Binary name: `jr`, package name: `jr`.

## Architecture

Single-crate thin client wrapping Jira REST API v3 and Agile REST API directly with reqwest. No generated client or intermediate abstraction layer.

```
src/
├── main.rs              # Entry point, tokio runtime, clap dispatch, Ctrl+C handling
├── cli/                 # Clap derive definitions + command handlers
│   ├── mod.rs           # CLI enums, global flags (--output, --project, --profile, --no-input, --no-color)
│   ├── issue/           # issue commands (split by operation theme)
│   │   ├── mod.rs           # dispatch + re-exports
│   │   ├── format.rs        # row formatting, headers, points display
│   │   ├── list.rs          # list only (JQL composition, filter application); `--component` filter (bare/`not:`/`all:`/`none`, resolved via §8.4, S-606-1)
│   │   ├── view.rs          # issue view handler, detailed single-issue rendering (~287 LOC)
│   │   ├── comments.rs      # comment list formatting and display (~61 LOC)
│   │   ├── create.rs        # issue create (platform path + JSM dispatch fork); parse_field_kv helper
│   │   ├── edit.rs          # issue edit: single + bulk field/label/type paths, dry-run, type-error enrichment; extracted from create.rs (Seam B, ADR-0012)
│   │   ├── jsm_create.rs    # JSM `handle_jsm_create` path + RT-id resolution (extracted from create.rs, ADR-0012/ADR-0014)
│   │   ├── workflow.rs      # move + transitions + assign + open
│   │   ├── interactions.rs  # comment CRUD handlers: add, delete, edit (body + visibility flags --internal/--public/--yes), view (S-577-1..6)
│   │   ├── links.rs         # link + unlink + link-types
│   │   ├── helpers.rs       # team/points resolution, user resolution, prompts
│   │   ├── assets.rs        # linked assets (issue→asset lookup)
│   │   ├── changelog.rs     # issue changelog handler (`jr issue changelog`)
│   │   ├── field_resolve.rs # field resolution helpers for `issue edit --field`
│   │   ├── attachments.rs   # attachment list handler + display_sanitize_filename (CWE-116, S-576-1)
│   │   └── json_output.rs   # JSON output helpers for issue commands
│   ├── assets/          # assets commands (module directory)
│   │   ├── mod.rs           # dispatch + re-exports
│   │   ├── search.rs        # assets search (AQL, enrichment)
│   │   ├── view.rs          # asset detail view
│   │   ├── tickets.rs       # connected tickets (`filter_tickets` lives here)
│   │   └── schemas.rs       # schema/type/attribute discovery
│   ├── auth/            # auth commands (module directory)
│   │   ├── mod.rs           # dispatch + re-exports
│   │   ├── keychain.rs      # keychain helpers
│   │   ├── list.rs          # auth list
│   │   ├── login.rs         # auth login
│   │   ├── logout.rs        # auth logout
│   │   ├── refresh.rs       # auth refresh
│   │   ├── remove.rs        # auth remove
│   │   ├── status.rs        # auth status (human text only; no JSON path)
│   │   ├── switch.rs        # auth switch
│   │   └── tests/           # inline integration tests + insta snapshots
│   ├── api.rs           # API passthrough command (`jr api`)
│   ├── board.rs         # board list/view
│   ├── sprint.rs        # sprint list/current/add/remove (scrum-only, errors on kanban)
│   ├── worklog.rs       # worklog add/list
│   ├── team.rs          # team list (with cache + lazy org discovery)
│   ├── user.rs          # user search/list/view (thin wrapper over api/jira/users.rs)
│   ├── init.rs          # Interactive setup (prefetches org metadata + team cache + story points field)
│   ├── project.rs       # project fields (types, priorities, statuses, CMDB fields)
│   ├── component.rs     # jr component list/create/edit/delete (~1066 LOC; see Known Size Deviations)
│   ├── queue.rs         # queue list/view (JSM service desks)
│   └── requesttype.rs   # requesttype list/fields (JSM request-type discovery + 7d cache)
├── api/
│   ├── mod.rs                 # module re-exports for api/
│   ├── client.rs              # JiraClient — HTTP methods, auth headers, rate limit retry, 429/401 handling
│   ├── auth.rs                # OAuth 2.0 flow + per-profile keychain layout (namespaced <profile>:email / <profile>:api-token / <profile>:oauth-access-token / <profile>:oauth-refresh-token; shared flat oauth_client_id/oauth_client_secret only); lazy migration of legacy flat OAuth keys for the "default" profile
│   ├── auth_embedded.rs       # thin sibling to auth.rs; XOR-obfuscated embedded OAuth app credentials
│   ├── pagination.rs          # Offset-based (most endpoints) + cursor-based (JQL search)
│   ├── rate_limit.rs          # Retry-After parsing
│   ├── refresh_coordinator.rs # per-profile single-flight OAuth refresh coordinator (prevents concurrent invalid_grant races)
│   ├── assets/          # Assets/CMDB API call implementations
│   │   ├── mod.rs           # module re-exports for api/assets/
│   │   ├── workspace.rs     # workspace ID discovery + cache
│   │   ├── linked.rs        # CMDB field discovery, asset extraction/enrichment (per-field + JSON)
│   │   ├── objects.rs       # AQL search, get object, resolve key
│   │   ├── schemas.rs       # schema discovery + object-type attributes (~44 LOC)
│   │   └── tickets.rs       # connected tickets
│   ├── jira/            # Jira-specific API call implementations (one file per resource)
│   │   ├── mod.rs       # module re-exports for api/jira/
│   │   ├── issues.rs    # search (full + keys-only), get, create, edit, list comments
│   │   ├── bulk.rs      # bulk issue operations (transition, field edit, label edit)
│   │   ├── boards.rs    # list boards, get board config
│   │   ├── sprints.rs   # list sprints, get sprint issues
│   │   ├── fields.rs    # list fields, story points + CMDB field discovery
│   │   ├── statuses.rs  # get all statuses (global, not project-scoped)
│   │   ├── links.rs     # create/delete issue links, list link types
│   │   ├── resolutions.rs # resolution list endpoint
│   │   ├── teams.rs     # org metadata (GraphQL), list teams
│   │   ├── worklogs.rs  # add/list worklogs
│   │   ├── projects.rs  # project details
│   │   ├── attachments.rs # list attachments on an issue (S-576-1; GET ?fields=attachment)
│   │   ├── components.rs # list/get/create/edit/delete component API calls (ADR-0018)
│   │   └── users.rs     # current user, user search, assignable users, single-user lookup
│   └── jsm/             # JSM-specific API call implementations
│       ├── mod.rs           # module re-exports for api/jsm/
│       ├── servicedesks.rs  # list service desks, project meta orchestration
│       ├── queues.rs        # list queues, get queue issues
│       ├── request_types.rs # JSM request-type discovery
│       ├── requests.rs      # JSM request creation (`handle_jsm_create` path)
│       └── attachments.rs   # JSM two-step attachment upload (attachTemporaryFile / request attachment)
├── types/
│   └── mod.rs           # module re-exports (assets, jira, jsm sub-modules)
├── types/assets/        # Serde structs for Assets API responses
│   ├── mod.rs           # re-exports
│   ├── linked.rs        # LinkedAsset + CMDB field types
│   ├── object.rs        # AssetObject, attribute value structs
│   ├── schema.rs        # schema/object-type discovery types
│   └── ticket.rs        # ConnectedTicket
├── types/jira/          # Serde structs for Jira API responses
│   ├── mod.rs           # re-exports
│   ├── issue.rs, board.rs, sprint.rs, user.rs, team.rs, project.rs, worklog.rs  # core types
│   ├── bulk.rs          # serde structs for bulk operations
│   ├── changelog.rs     # serde structs for changelog API
│   ├── component.rs     # Component, RelatedIssueCounts serde types (ADR-0018)
│   └── editmeta.rs      # serde structs for editmeta
├── types/jsm/           # Serde structs for JSM API responses
│   ├── mod.rs           # re-exports
│   ├── servicedesk.rs   # ServiceDesk, project meta types
│   ├── queue.rs         # Queue, queue issue list types
│   └── request_type.rs  # RequestType, request type fields types
├── cache.rs             # Per-profile XDG cache (~/.cache/jr/v1/<profile>/) — team list, project meta, workspace ID, CMDB fields, object-type attrs, resolutions (all 7-day TTL). Versioned root (`v1/`) lets a future schema bump orphan stale files cleanly. Also holds `{read,write,invalidate}_components_cache` — ADR-0018 §2 foundation for `jr component rename` (S-608-1), not yet wired into any read/resolve path this cycle (see the rustdoc on those functions).
├── config.rs            # Global (~/.config/jr/config.toml) [profiles.<name>] + default_profile + per-project (.jr.toml), figment layering. Auto-migrates legacy [instance]/[fields] shape on first load. Active profile resolved at load via Config::load_with(cli_profile) (cli flag threaded through as a parameter, NOT an env-var seam) > JR_PROFILE env > default_profile field > "default".
├── output.rs            # Table (comfy-table) and JSON formatting
├── adf.rs               # Atlassian Document Format: text→ADF, markdown→ADF, ADF→text
├── duration.rs          # Worklog duration parser (2h, 1h30m, 1d, 1w)
├── observability.rs     # --verbose / --verbose-bodies flag helpers, eprintln! wrappers (~39 LOC)
├── lib.rs               # Crate root (re-exports for integration tests)
├── jql.rs               # JQL utilities: escaping, validation, asset clause builder
├── partial_match.rs     # Case-insensitive substring matching with disambiguation
└── error.rs             # JrError enum with exit codes (0/1/2/64/78/124/130)
```

Product-namespaced `api/jira/` and `types/jira/` so future JSM/Assets support adds sibling directories.

## Known Size Deviations

- `cli/issue/list.rs`: ~2,012 LOC (re-measured 2026-08-25; was documented as 1,256 LOC post-split, target was ≤750 per `docs/specs/list-rs-split.md`; spec target not achieved but split was partial — `view.rs` and `comments.rs` already extracted). Growth since the prior figure is attributable to the list-read-ergonomics additions (`--fields`, `--updated-recent`, `--sort`, `--component` — BC-2.1.023/024/025, BC-2.2.033, BC-2.3.041/042). NFR-O-G: DOCUMENT-AS-IS-COMPLETE (S-3.08).
- `cli/issue/create.rs`: ~1,253 LOC (re-measured 2026-08-31, FIX-F7-001; was ~530 LOC at the 2026-08-25 measurement, which was itself documented as 394 LOC post-Seam-B split; was 2,447 pre-split, edit cluster extracted to `edit.rs`) — CROSSES ADR-0012's 1,000-LOC shard threshold. Growth since the 530-LOC figure is attributable to the S-578-1 `parse_field_kv` hint-syntax changes and S-578-4's platform-path `--field` createmeta resolution (BC-3.3.010/BC-3.3.011, DEC-310): the step 2a hint-parse guard, the step 2b `CREATE_D2_GOVERNED_KEYS` dedicated-flag × `--field` collision guard, and the step 4b `get_issue_types_for_project` + `get_createmeta_fields` resolution call that reuses `field_resolve.rs`'s `dispatch_field_value` via the `FieldMetaSource::Create` arm — plus the DEC-310 guard reversal removing S-639-1/DEC-188's `--field`-alone pre-flight exit. Handles `handle_create`'s platform create path + JSM dispatch fork + `parse_field_kv` + these S-578-4 additions. Single command-family entry point with dual platform/JSM dispatch and its own createmeta-resolution orchestration — same DOCUMENT-AS-IS rationale as `component.rs`/`attachments.rs`/`field_resolve.rs` for staying as one file rather than fragmenting one command's control flow across shards. DOCUMENT-AS-IS. ADR-0012. (PF-016, S-578-4, FIX-F7-001)
- `cli/issue/edit.rs`: ~3,187 LOC (Seam B extraction of `handle_edit` cluster from `create.rs`, originally 2,067 LOC; grew via S-605-1/S-605-2's single-key and bulk `--component` paths — BC-3.4.022/BC-3.4.023 — plus the Step-4.5 Round-1 through Round-3 adversarial-review fixes on top of S-605-2 (including the Round-3 F2 fix consolidating the `--dry-run` multi-key path's component-list fetch into a single `GET`), ~3× the ADR-0012 1,000-LOC threshold). Handles `issue edit`: single-key + bulk field/label/type/component paths, dry-run, type-error enrichment. ADR-0012. (PF-016)
- `cli/issue/workflow.rs`: ~1,277 LOC — covers move/transitions/assign/open; 28% over the threshold. `handle_comment` extracted to `interactions.rs` (S-577-1). DOCUMENT-AS-IS. ADR-0012. (PF-017)
- `cli/component.rs`: ~1,800 LOC — covers `jr component list/create/edit/delete/rename` (S-604-1/2/3, S-608-1 added `rename`'s single-project + `--all-projects` fan-out + `--dry-run` forms, ~700 LOC), ~80% over ADR-0012's 1,000-LOC shard threshold. ADR-0018 §1/§2 anticipated this size (numeric-vs-name resolution paths, disposition-required delete, snapshot-before-delete safety, and ADR-0018 §2 cache-invalidation call sites are all in this one file by design, for a single command family). DOCUMENT-AS-IS. (F5-C-002)
- `cli/issue/attachments.rs`: ~3,472 LOC (measured 2026-08-25; previously undocumented) — over 3× the ADR-0012 1,000-LOC threshold. Covers the full attachment command family in one file: list, upload (platform + JSM two-step `--public`/`--internal` paths, multipart retry rebuild), download (two-step metadata+content, redirect/CDN handling), delete (targeted + bulk, dry-run, replace-existing), plus the CWE-22/CWE-116 filename-sanitization helpers (`sanitize_attachment_filename`, `display_sanitize_filename`) documented elsewhere in this file. Single command family with dual platform/JSM dispatch, mirroring `component.rs`'s rationale for staying as one file. DOCUMENT-AS-IS. (S-CLEANUP-2026-08-25)
- `cli/mod.rs`: ~1,356 LOC (measured 2026-08-25; previously undocumented) — clap derive definitions for every subcommand/flag across the whole CLI surface (auth, issue, board, sprint, worklog, team, user, project, component, queue, requesttype, assets, api). Single derive tree by design; splitting would fragment `#[derive(Subcommand)]` enums across files with no natural seam. DOCUMENT-AS-IS. (S-CLEANUP-2026-08-25)
- `cli/issue/helpers.rs`: ~1,113 LOC (measured 2026-08-25; previously undocumented) — team/points resolution, user resolution, and interactive prompt helpers shared across the `issue` command family. DOCUMENT-AS-IS. (S-CLEANUP-2026-08-25)
- `cli/issue/field_resolve.rs`: ~1,635 LOC (re-measured 2026-08-30, S-578-4; was ~1,270 LOC at S-578-2) — crossed ADR-0012's 1,000-LOC shard threshold in S-578-2 and grew further in S-578-4. Now backs TWO command families, not one: `issue edit --field` (the bare-form dispatch — string/number/option/date/datetime/user — the S-578-2 hinted-bypass composers `:option` non-cascading and cascading `>`-split/`:id`/`:name`/`:asset` workspace-scoped CMDB references, the EC-3.4.027-1 entry-point `schema.type` gate, and the cascading-error branches EC-3.4.027-2/3/6/7) AND `issue create --field` (S-578-4, BC-3.3.010/011, DEC-310) via `resolve_against_createmeta`, the ten-member `CREATE_D2_GOVERNED_KEYS` collision guard, and `detect_flag_field_overlap`. The two command families share ONE dispatch implementation (`dispatch_field_value`, Architecture Compliance Rule 1/AC-008) rather than duplicating the type-dispatch algorithm — this is why the file grew instead of splitting: the resolution algorithm is cohesive across both call sites, mirroring `component.rs`'s and `attachments.rs`'s rationale for staying as one file. DOCUMENT-AS-IS. (S-578-2, S-578-4)

## Build & Test

```bash
cargo build                          # Build debug
cargo build --release                # Build optimized (LTO, strip, panic=abort)
cargo test                           # All tests (unit, integration, proptest, snapshots)
cargo test --lib                     # Unit tests only
cargo test --test '*'                # Integration tests only
cargo clippy -- -D warnings          # Lint (zero warnings policy)
cargo fmt --all -- --check           # Format check
cargo deny check                     # License + vulnerability audit
DIFF_FILE=$(mktemp -t pr.diff.XXXXXX) && trap 'rm -f "$DIFF_FILE"' EXIT && git diff origin/develop...HEAD > "$DIFF_FILE" && cargo mutants --in-diff "$DIFF_FILE" --jobs 4 --timeout 240  # Mutation testing on PR diff scope (see docs/specs/cargo-mutants-policy.md)
```

## Conventions

- **Commits:** Conventional Commits format (`feat:`, `fix:`, `docs:`, `chore:`, `ci:`, `test:`)
- **Branches:** `type/short-description` (e.g., `feat/issue-commands`, `fix/auth-flow`). Default branch is `develop`. Feature branches → PR to `develop` → PR to `main` for releases.
- **Protected branches:** `main` and `develop` require CI to pass and code owner approval on PRs. Admins can bypass.
- **`strict: false` on both protected branches — tested tree ≠ merged tree (ADV-P50-MED-002, documented not changed, round 20):** Neither `main` nor `develop`'s branch-protection rule requires a PR branch to be up to date with its base before merging (`required_status_checks.strict` is `false` on both). Consequence: `ci-gate`'s green result is computed against the base branch's state AT THE TIME the check last ran on that PR — not re-validated against the base branch's current tip immediately before merge. A PR opened against `develop` at commit A, whose gate went green there, can still merge cleanly even after `develop` has since moved to commit B — the code that actually lands on `develop` is the PR branch merged onto B, a combination that was never itself CI-tested. This is a real, observed condition, not a hypothetical: during this same session PR #667 sat green against a stale `develop` until it was manually reconciled with `develop`'s current tip before merge. Practical implication for anyone merging a PR that has been open for a while: re-run (or at least re-check the age of) the gate if `develop`/`main` has moved meaningfully since the PR's last green run, rather than trusting an old green check by itself. **Do not flip `strict: true` without the human's explicit sign-off** — it is a repo-wide merge-behavior change (every PR would need a rebase/merge-commit-and-rerun cycle before merging, not just this repo's CI-gate machinery), and that decision is intentionally still pending, not made by this note.
- **CI Gate — SCOPE SUMMARY (read this before the history below):**
  **Enforced:** decision semantics (behavioral subprocess tests against `scripts/check-ci-gate.sh`, no extraction layer to under-report) · assertion shapes (job/step key sets, `if:`/env value pins — set-equality, default-deny) · value substitution (byte-for-byte pins that fail LOUD via `Err` on anything they can't confidently normalize) · non-LF YAML line breaks (complete for that one class: CR, NEL, LINE SEPARATOR, PARAGRAPH SEPARATOR) · **(S-CIGATE-3, 2026-08-11) YAML node properties on a pinned key (`ci-gate` job block only)** — `&anchor`/`!tag`/`!!tag` prefixing a mapping key, the round-16 residual below, is now caught structurally by a real event-stream parse (`tests/common/wf.rs::find_key_node_properties`, asserted in `tests/ci_gate_completeness.rs`'s M2-q / AC-007), not merely documented as an accepted risk.
  **NOT enforced:** `uses:` VALUES on the decision path (the two `uses:` steps inside `ci-gate` remain unpinned — see the `$GITHUB_ENV`/`$GITHUB_PATH`/sudo residuals in the round-13 history below, unaffected by S-CIGATE-3); the node-property class for jobs OTHER than `ci-gate` (`mutants`/`msrv`/`spec-guard`/etc. — narrowly, only for an anchor/tag on a key that is ALREADY a legitimate pinned member; a NEW anchored key is still caught by those jobs' ordinary key-set pins); human judgment on `PINNED_ALLOWED_SKIP_IF_EXPRESSIONS`/other pin updates. **The line-based LEXER layer's node-property gap that previously headlined this line is CLOSED as of S-CIGATE-3 (2026-08-11)** — see the S-CIGATE-3 paragraph after the round-16 history below for exactly what changed and what deliberately did not.
  **Control for the unenforced:** code review of the `ci.yml` diff, scoped now to `uses:` VALUES and `ALLOWED_SKIPS`/pin-literal human judgment specifically — the node-property bypass this line used to describe is no longer in this category (S-CIGATE-3).
  **Durable fix — LANDED (S-CIGATE-3, 2026-08-11), not merely re-promised:** `ci.yml` is now parsed once with a real YAML parser (`saphyr-parser` 0.0.11's low-level event stream, via `tests/common/wf.rs`) and structural assertions query the parsed tree; today's byte pins are retained as a second layer sourced from parsed `Event::Scalar` values, not replaced or weakened. See the S-CIGATE-3 paragraph after the round-16 history below for the full account, including what this explicitly did NOT close.
  **Review scope is three files, not one (S-626-1 pass-54, ADV-P54-LOW-001) — now four as of S-CIGATE-3 (2026-08-11):** `.github/workflows/ci.yml`, `scripts/check-ci-gate.sh`, `tests/ci_gate_completeness.rs`, and (added by S-CIGATE-3) `tests/common/wf.rs` are all PR-supplied and executed from that PR's own checkout — a malicious or careless PR can edit any of the four in the same change. No repository check compares any of them against an outside-the-tree reference definition (there is no "golden copy" of the gate elsewhere in the repo, in branch protection settings, or in a separate CI job); the guarantees documented above (byte pins, key-set pins, the fixture suite, `EXPECTED_FIXTURES`) are all self-referential to whatever these files currently say — `tests/common/wf.rs` is now the single choke point every structural guard in `tests/ci_gate_completeness.rs` parses through, so a bug planted there (not only in `ci.yml` itself) could silently under-report the same way `extract_key_name_at_indent`/`collect_mapping_key_set` once did. Reviewing `ci.yml` alone and treating the other three files as pre-verified infrastructure is a gap — code review of a `ci-gate`-touching PR must cover the diff across all four files together.
  ---
  `ci-gate` (job name "CI Gate") is THE single required branch-protection status check on `develop`/`main`. New CI jobs that must be required must be added to `ci-gate.needs`, never wired directly into branch protection — this prevents the matrix-rename fragility class (DEC-096/DEC-097). `ci-gate`'s pass/fail decision is fail-closed (`scripts/check-ci-gate.sh`, S-CIGATE-2): every job in `needs` must report `success`, except jobs named in the script's `ALLOWED_SKIPS` allowlist (currently `mutants` only), which may additionally report `skipped` — any other result (including a future GitHub Actions conclusion type never seen before) fails the gate by default. A job added to `ci-gate.needs` that can legitimately report `skipped` (e.g. a future PR-only or repo-variable-gated job) must ALSO be added to `ALLOWED_SKIPS`, or the gate will correctly start failing that job's push-event runs — enforced by the script's fail-closed default, not by convention alone. `ALLOWED_SKIPS` membership by itself is NOT the trust boundary. Rounds 1-2 of PR #671 review guarded `ALLOWED_SKIPS`'s bash-array REPRESENTATION (source-text parsing, then an occurrence count plus a `--print-allowed-skips` shell-out) — that half is now solid. Separately, TWO successive predicate-based attempts to answer "is this job's `ci.yml` `if:` expression legitimate?" from source text alone (round 3: presence-only; round 5, "M1": a no-op blacklist plus an event/config-marker requirement) were each bypassed — PR #671 review round 6 concluded that question is undecidable by pattern-matching and replaced it with a decidable one: is the job's `if:` expression BYTE-IDENTICAL (after narrow, reject-don't-parse normalization — see `tests/ci_gate_completeness.rs::extract_and_normalize_if_expr`, hardened round 7 to reject YAML block-scalar `if:` forms, line continuations, and ambiguous embedded `#` characters rather than silently mis-parsing them) to a literal a human has actually reviewed and pinned in `tests/ci_gate_completeness.rs::PINNED_ALLOWED_SKIP_IF_EXPRESSIONS` (currently one entry: `mutants` → `github.event_name == 'pull_request'`)? `test_ci_gate_decision_matches_job_level_if_for_every_needs_member` runs `scripts/check-ci-gate.sh` as a real subprocess against PRODUCTION-SHAPED synthesized payloads (every job carries exactly `result`+`outputs` — the two properties GitHub's contexts reference documents for the `needs` context, no more; round 7 briefly modeled a third, phantom `outcome` field that belongs only to the `steps` context and was removed in round 8, see `tests/ci_gate_completeness.rs::build_multi_skip_payload` and its `NEEDS_CONTEXT_JOB_KEYS` structural pin added round 9 — a runtime assertion that fails loudly if a future round invents or drops a field here without checking the docs first) for every `ci-gate.needs` job, checked both alone and alongside `mutants` also skipped, and asserts the gate's actual exit code AND diagnostic-line content match whether the job's `if:` matches its pin — widening `ALLOWED_SKIPS` to a job with no matching pinned entry fails this test loudly, naming the job and requiring a human-reviewed pin added in the same change. A sibling test, `test_ci_gate_decision_is_arity_independent_for_unlisted_skips` (round 9), additionally skips EVERY `ci-gate.needs` job simultaneously in one payload — closing a gap where a mutation keyed on skip-COUNT (e.g. "≥3 skips tolerates everything") was invisible to the 1- and 2-skip payloads above; testing the extremal (all-jobs) arity refutes any such count threshold up to the current job count without needing the full 2^N cross product. The same suite's pin-coverage check (`test_ci_gate_decision_matches_job_level_if_for_every_needs_member`'s final block) no longer trusts `--print-allowed-skips` alone (round 9, after that trust was shown exploitable: a mutation to `print_allowed_skips()` alone, filtering its output, desyncs it from `is_allowed_skip()` — the function `evaluate_needs()` actually calls — without touching real enforcement) — it instead directly probes `evaluate_needs()` behaviorally, one job at a time, for every job name in ci.yml (`list_all_ci_yml_job_names`, not just `ci-gate.needs` members), requires a pin for every job real enforcement tolerates a skip for, and separately asserts that behaviorally-derived set matches `--print-allowed-skips`'s report. This check's actual unique value (round 9 correction of an inaccurate round-7 claim, verified by reproduction) is for an `ALLOWED_SKIPS` member that is NOT a `ci-gate.needs` member (e.g. `security`) — the main per-job loop already independently requires a pin for any `needs` member that's skip-tolerant, so for a `needs` member this check is redundant-but-harmless, not the source of that guarantee. `extract_and_normalize_if_expr` also rejects (round 9) a job block containing two job-level `if:` keys (defense-in-depth only — GitHub Actions and `actionlint` both already reject duplicate mapping keys at parse time), and (round 9, fixing a false-red) no longer treats a full-line YAML comment following a genuinely single-line `if:` as a folded continuation — only a line with real, non-comment content past the indent threshold counts. Two older tests (`test_allowed_skips_members_require_job_level_conditional_in_ci_yml`, shelling out to `--print-allowed-skips`; `test_allowed_skips_has_exactly_three_code_level_references`, counting three textual shapes) remain as faster diagnostics but are not sufficient alone. The pre-existing `test_ci_gate_needs_jobs_have_no_job_level_if` (renamed round 20, ADV-P48-LOW-001, from `test_ci_gate_needs_jobs_have_no_event_conditional_if`) provides additional, narrower, LINE-SCOPED coverage directly on `ci.yml`'s `if:` text: it rejects the PRESENCE of a job-level `if:` key at all — not merely one referencing `github.event_name` — on the seven named always-run jobs (`fmt`, `clippy`, `test`, `msrv`, `deny`, `spec-guard`, `check-signing-workflow-injection`); it does not evaluate the expression's truth value and does not cover other jobs. **Correction (S-626-1 pass-55, ADV-P55-LOW-001):** an earlier revision of this sentence claimed the match was on "ANY job-level `if:` key at all… regardless of shape or content" — that overstated the KEY-spelling coverage specifically (as distinct from the accurate, separate VALUE-content-independence claim in the sentence that follows this one): the underlying match was a bare `line.starts_with("    if:")`, matching only that one literal spelling and missing `"if":`, `'if':`, and `if :` (space before colon) — all PyYAML-identical to the bare spelling. Both this test's own detection (`test_ci_gate_needs_jobs_have_no_job_level_if`) and `extract_and_normalize_if_expr`'s internal `is_job_level_if_line` closure now route through `extract_key_name_at_indent`, the same quote/whitespace-aware matcher used everywhere else in this file, closing that gap for those three additional spellings — still bounded by this file's documented round-16 residual (a YAML node property such as `&anchor`/`!tag` prefixing the key defeats `extract_key_name_at_indent` itself, so "ANY" remains inaccurate as a literal claim even post-fix). This closes the block-scalar escape by construction rather than incidentally: matching on the `if:` KEY's presence at job-property indent, regardless of the condition's shape or content, still catches a folded/block scalar (`if: >-` with the condition on continuation lines) because its opening line still starts with `    if:` even though the condition text itself lives on later lines — the same escape `extract_and_normalize_if_expr` independently rejects outright for `ALLOWED_SKIPS` pin evaluation. The residual limitation of the round-6/7 pinned-literal design is not a known expression that defeats it — a human reviewer approving a new pinned entry could still misjudge whether an expression genuinely evaluates both `true` and `false` in practice, but that judgment call is now an explicit, required, visible step (a pinned-literal diff reviewed like any other code change), not something delegated to an automated predicate. **Round 10 CRITICAL:** every prior round's finding made the gate tolerate an illegitimate SKIP; this one made it tolerate every FAILURE. `test_ci_gate_pass_fail_semantics_are_structurally_placed`'s M2-g/h assertions checked only that a `run:` step exists and mentions `check-ci-gate.sh` + `toJSON(needs)` as substrings — an allowlist of known-good substrings with no default-deny on the run line itself, the same shape as the retired inline condition one layer up. Four one-line edits (`|| true` appended to the run line; `continue-on-error: true` at step or job level; piping the invocation through `| cat`) each left the gate's own test suite at 14/14 green while making it tolerate every upstream failure. New M2-i pins the gate step's `run:` line byte-for-byte (via `tests/ci_gate_completeness.rs::extract_and_normalize_sole_run_line`, a deliberately separate function from `extract_and_normalize_if_expr` rather than a generalization of it — reject-don't-parse normalization is duplicated, not shared, to avoid risking the more heavily-bypassed function); new M2-j rejects the literal substring `continue-on-error` anywhere in the `ci-gate` job block (step or job level) — it has no legitimate use there, and `continue-on-error: true` is already an established idiom elsewhere in this same file (`ci.yml :: mutants`), so on `ci-gate` specifically it would read as unremarkable in a diff. **Round 10 IMPORTANT (outputs-content guard, round 9):** the round-9 no-job-declares-`outputs:` guard matched only the bare `outputs:` spelling; PyYAML-confirmed `"outputs":`, `'outputs':`, and `outputs :` (space before colon) all parse to the identical key, so a real job-level `outputs:` block spelled any of those three left the guard silently blind. `tests/ci_gate_completeness.rs::line_declares_job_level_key` now recognizes all four spellings (and drops a dead conjunct the original had — `&& !l.starts_with("        ")` could never be false once the 4-space-prefix check had already matched). **Round 10 IMPORTANT (fixture-count pin):** `scripts/check-ci-gate.sh --self-test`'s own summary line reported its shrunken denominator as success when a fixture was silently deleted (12/12, 11/11) — mirrors the `EXPECTED_FIXTURES`/`fixtures_run` pin already used by `scripts/check-bc-citation-symbols.sh` and `scripts/check-cargo-mutants-policy-citations.sh`; `check-ci-gate.sh` now pins `EXPECTED_FIXTURES=13` the same way. **Round 11 CRITICAL (3 findings, same diagnosis):** round 10's M2-i pinned the run line's VALUE but left the surrounding KEYS an open enumeration — round 10 closed exactly one member (`continue-on-error`); three more were open, each a one-line `ci.yml` edit leaving the suite 14/14 green: (1) fabricating the `env: NEEDS_JSON:` value instead of `${{ toJSON(needs) }}` — worse than `|| true`, since it makes `check-ci-gate.sh`'s own log read as a legitimate all-green run instead of a suppressed real failure, and the pre-existing AC-001/M2-h substring checks (`toJSON(needs)` appearing anywhere in the block) are satisfied by a decoy comment; (2) `ci-gate`'s OWN `if:` was never pinned — `if: ${{ always() && false }}` and `if: ${{ always() && github.ref == 'refs/heads/nonexistent' }}` (the latter VERBATIM the always-FALSE bypass `ALLOWED_SKIPS`'s own comment cites from round 5, previously applied only to `needs` members) both contain `always()` and lack `contains(needs`, so M2-a/b/c pass while the job never runs — S-CIGATE-2's founding premise, reopened; (3) `shell:` was unguarded, both step-level (`shell: cat {0}` on the gate step — GitHub accepts a custom shell TEMPLATE `command [options] {0}`, so the runner `cat`s the run line's script body instead of executing it) and workflow-level (`defaults: run: shell: cat {0}`, which lives OUTSIDE every job block and is invisible to any `extract_job_block`-anchored assertion by construction). Closed via the "minimum equivalent" to a full-block pin (assessed and rejected as impractical — see `tests/ci_gate_completeness.rs`'s round-11 module comment for why): new M2-k/l pin the job's and every step's COMPLETE key sets (`PINNED_GATE_JOB_KEYS`, `PINNED_GATE_STEP_KEY_SETS`) so ANY added/removed/renamed key on the job or any step fails, not just named exceptions; new M2-m reuses `extract_and_normalize_if_expr` (previously applied only to `needs` members) against a new `PINNED_GATE_IF_EXPR` (`${{ always() }}`) for `ci-gate`'s own `if:`; new M2-n pins the `NEEDS_JSON:` env-child line byte-for-byte via a new `extract_and_normalize_sole_needs_json_line` (same reject-don't-parse rules as the run-line function, deliberately separate for the same reason that one is separate from `extract_and_normalize_if_expr`); a new `test_ci_yml_has_no_workflow_level_shell_override` reads `ci.yml` at the WORKFLOW level (the only test in this file that does) for the `defaults:` vector, since `common::yaml::extract_job_block`'s doc comment (corrected round 11 to state it guarantees anchoring, not coverage) explains why no job-scoped check can ever see it. **Round 12 (2 CRITICALs + 1 IMPORTANT, all small fixes; round 11's job/step key-set pins independently attacked with 14 constructions and confirmed genuinely default-deny — not touched):** CRITICAL 1 — `test_ci_yml_has_no_workflow_level_shell_override`'s original `defaults:` detection was `ci.lines().any(|l| l == "defaults:")`, exact LINE equality rather than key detection (round 10's `outputs:` finding, one level up) — flow style, a quoted key, a trailing comment, and a trailing space all passed; fixed to reuse `extract_key_name_at_indent(l, 0)`, the SAME quote/whitespace-aware matcher round 11 had already built for job/step keys, just not called here. CRITICAL 2 — the gate step's `env:` mapping was an unpinned open set under a pinned key: M2-n (round 11) pins `NEEDS_JSON:`'s value but never asserted it was the ONLY child; a sibling `BASH_ENV` (step-level, or appended to the WORKFLOW's own top-level `env:` block one level further out — both 15/15 green) is independently exploitable, verified under the runner's actual non-interactive-bash invocation flags: `BASH_ENV` is sourced regardless of `--norc`/`--noprofile`, so a shim ending in `exit 0` ends the shell before the pinned run line's body executes. Fixed via new M2-o (`PINNED_GATE_ENV_KEYS`, gate step's `env:` key set at indent 10) and a new `test_ci_yml_workflow_level_env_key_set_is_pinned` (`PINNED_WORKFLOW_ENV_KEYS`, the workflow's own top-level `env:` key set at indent 2 — the second workflow-level-scoped test in this file, alongside the `defaults:` one). IMPORTANT — round 11's "minimum equivalent to a full-block pin" framing, and its panic text, read as though the whole `ci-gate` block were closed; it explicitly is not — `tests/ci_gate_completeness.rs`'s round-11/12 module comment now states PRECISELY which levels are pinned (job/step key NAMES, `if:` VALUE, `NEEDS_JSON`/env key sets, `defaults:`/`continue-on-error` PRESENCE) versus NOT pinned (`uses:` VALUES, `with:` block CONTENTS, `name:` VALUES — deliberately out of scope for this story, which is about the pass/fail decision path, not supply-chain pinning). Also corrected round 11's documented indent-re-indentation limitation: reviewed round 12 attacked it directly (re-indenting `ci-gate`'s whole child block +2, PyYAML-confirmed legal) and found it fails CLOSED, not open — M2-a's PRESENCE check for `    if:` at the exact indent fires first, before any deeper pin is reached. Round 12 recorded a general rule in the module comment — "presence-shaped checks fail closed under position drift; absence-shaped checks with a position assumption fail open" — that round 13 found to be wrong, self-corrected by the reviewer who had repeated the narrower version both to the implementer and to two other reviewers before catching it: M2-o and the workflow-level env-key-set test are neither presence- nor absence-shaped, they're SET-EQUALITY checks, and round 13 showed both fail open (see Round 13 CRITICAL below). Corrected rule: any check whose EXTRACTOR can silently under-report its input fails open, regardless of whether the assertion built on that extraction is presence-, absence-, or set-equality-shaped — the assertion is only as strong as the extraction feeding it. Also improved (not loosened) two benign-false-red messages: M2-g (a reordered step where `run:` becomes the dash-bearing first key) now explains the position-not-absence distinction and points to M2-l as the check that still confirms the step is intact; M2-i (a cosmetically re-quoted run line, e.g. wrapped in single quotes) now explains this checker treats the value as an opaque plain scalar and does not interpret YAML quoting, same tradeoff as `extract_and_normalize_if_expr`'s `${{ }}`-wrapper literalism. **Round 13 CRITICAL (env-key-set extraction under-reports past a comment):** `extract_gate_env_key_set` and `extract_workflow_env_key_set` (round 12) scanned each `env:` block's children with a `take_while` that TERMINATED on the first line that was neither blank nor at the exact expected child indent — a YAML comment is such a line at ANY indentation (YAML ignores comment indentation), so a comment inserted between the pinned `NEEDS_JSON:`/`CARGO_TERM_COLOR:` line and a smuggled sibling (e.g. `BASH_ENV:`) truncated the extracted key set BEFORE reaching the smuggled var — the pin still matched the truncated (too-small) set while the real, larger, exploitable set went undetected, with the full 16/16 suite staying green. Fixed via a shared `collect_mapping_key_set` helper that explicitly skips (never terminates on) blank and comment lines, only stopping at a line whose actual indent drops below the child indent; both extractors now route through it. Both call sites also gained a non-empty assertion on their result — previously a missing or mis-anchored `env:` block silently returned `Vec::new()`, and the check only "worked" by the accident of the pin itself being non-empty. **Round 13 IMPORTANT 1 (gate-env anchor):** `extract_gate_env_key_set` anchored to the FIRST indent-8 `env:` anywhere in the job block, not specifically the gate step's own — correct only because the step-key-set pin (M2-l) forbids an earlier step from having its own `env:`; a future legitimate change adding `env:` to an earlier step (with M2-l's pin updated correctly in the same commit) would silently repoint this function at the wrong step's `env:`, reopening the gate step's own `env:` as unpinned while the suite stayed green. Fixed: the function now anchors to the `env:` immediately preceding the SAME step's `run:` line (scanning backward from `run:`, bounded by that step's own `- ` marker). **Round 13 IMPORTANT 2 (`uses:` scope note corrected):** round 12's module comment justified leaving `uses:` VALUES unpinned as "supply-chain pinning of actions used elsewhere in the file" while citing, as its own example, swapping the checkout action — which is INSIDE the `ci-gate` job, on the decision path (both `uses:` steps in `ci-gate` run before the gate step and could, e.g., write `$GITHUB_ENV` — a documented GitHub Actions mechanism letting a step set env vars for later steps in the same job — reproducing the `BASH_ENV` CRITICAL without ever touching the gate step's own `env:` block; reachable only via `uses:`, since a `run:` step attempting this is already caught by the step-key-set arity pin). **`$GITHUB_PATH` is a second, equivalent mechanism at the same seam (S-626-1 research pass, 2026-08-10, Q-F(1), CONFIRM on primary source — GitHub Docs "Workflow commands for GitHub Actions" §"Adding a system path": prepending a directory to `PATH` "automatically makes it available to all subsequent actions in the current job"):** either `uses:` step could prepend a directory to `$GITHUB_PATH` ahead of the system location, so a later `run:` step's `command -v jq` (or any other bare binary invocation) resolves through the shim first — this is the mechanism underpinning `ADV-P59-LOW-001`'s `PATH`→`jq` shim vector (fixed at the call site in `f2bea32e`), and it was previously undocumented here alongside `$GITHUB_ENV`. The module comment now states plainly that these two `uses:` are on the decision path and knowingly left unpinned (this story is about the pass/fail decision path, not supply-chain pinning) — the `$GITHUB_ENV` mechanism is documented, not independently executed against a real runner as part of this story. **The sudo bound (S-626-1 research pass, 2026-08-10) — same residual as the two unpinned `uses:` values above:** GitHub Docs, "GitHub-hosted runners" § "Administrative privileges", states verbatim: "The Linux and macOS virtual machines both run using passwordless `sudo`." Whatever runs before the gate step in the `ci-gate` job does not need a `PATH`/`$GITHUB_PATH` shim at all — `sudo cp /tmp/shim /usr/bin/jq` replaces the trusted system binary in place, and no directory allowlist, however refined, can detect that, because the shim now IS the trusted path. `resolve_trusted_jq`'s directory allowlist (`trusted_jq_dirs_for`, dirname-immune, RUNNER_OS-keyed) therefore raises the cost of forging the gate's decision by exactly one word (`sudo`): it closes the cheap, likely-accidental vector (a third-party action mutating `PATH`) and fails closed, but it does not, and must never be described as, stopping a determined attacker with earlier-step execution — that property is unreachable from inside this script, same as the `uses:`-pinning gap. Detail: `.factory/research/ci-gate-shell-trust-assumptions-2026-08-10.md`. **Round 13 IMPORTANT 3 (`defaults:` detection, two more spellings; checker fix only, NOT an established end-to-end exploit):** `extract_key_name_at_indent(l, 0)` still missed a UTF-8 BOM prefix (`\u{FEFF}defaults:`) and YAML explicit-key syntax (`? defaults` / `: {run: {shell: "cat {0}"}}`). `read_ci_yml()` now strips a leading BOM once at file-read time (mirroring what a spec-compliant YAML parser does), and `extract_key_name_at_indent` now recognizes a `? <key>` explicit-key line without requiring a same-line colon. Both are reproduced as failing today (`test_ci_yml_has_no_workflow_level_shell_override`, RED before the fix, PASS after) and 16/16 stays green otherwise. Whether GitHub's own workflow parser (`actions/runner`, not PyYAML) actually accepts a BOM-prefixed workflow file or `? key` explicit-key syntax at all is UNVERIFIED — a targeted search returned nothing either way — so only the CHECKER's fidelity gap is established fact; end-to-end exploitability of either vector against real GitHub Actions is NOT claimed here. **Round 14 CRITICAL (one layer below every prior round — line-splitting itself, not any one extractor):** `read_ci_yml()` normalizes `\r\n` to `\n`, but every extractor in this file then operates on `str::lines()`, which splits ONLY on `\n`. YAML's own line-break set (YAML 1.1 `b-char`, which PyYAML implements) is larger: a lone CR (U+000D), NEL (U+0085), LINE SEPARATOR (U+2028), and PARAGRAPH SEPARATOR (U+2029) all end a LOGICAL line for a real YAML parser without splitting a `str::lines()` iteration, so a key placed after one of these on the same PHYSICAL text line is invisible to every line-based check in this file SIMULTANEOUSLY — `extract_key_name_at_indent`, `extract_job_level_key_set`, `extract_gate_step_key_sets`, `collect_mapping_key_set`, and both workflow-level env-key-set tests. Three genuine one-line diffs, each PyYAML- AND Ruby Psych/libyaml-confirmed legal (independent implementations ruling out a single-parser quirk), actionlint-clean, and (`.gitattributes`' `text eol=lf` normalizes CRLF pairs but does not strip a lone CR or the Unicode variants) confirmed to survive a real commit, each left the full 16-test suite green: (1) a lone CR between `CARGO_TERM_COLOR: always` and a smuggled `BASH_ENV:` reopens round 12's workflow-env CRITICAL for every job in the file; (2) a lone CR between `name: CI` and `defaults: {run: {shell: "cat {0}"}}` reopens round 11's workflow `defaults:` CRITICAL; (3) a CR appended to the gate step's `name:` line followed by `        shell: cat {0}` defeats `PINNED_GATE_STEP_KEY_SETS` — the step-key-set pin confirmed default-deny against fourteen constructions in round 12, but only over the lines `str::lines()` actually produced; PyYAML/Ruby both parse the gate step's keys as `["env", "name", "run", "shell"]` with `shell: "cat {0}"`. NEL independently reproduces exploit 2 identically; LINE SEPARATOR and PARAGRAPH SEPARATOR were each separately confirmed exploitable the same way. Fixed via a new, dedicated `test_ci_yml_contains_no_non_lf_yaml_line_breaks`: a byte-level scan of the raw file for all four characters, naming the offending byte and its Unicode codepoint on failure — a MITIGATION, not the durable fix; the durable fix is parsing `ci.yml` once with a real YAML parser and asserting over the parsed tree for every structural check, keeping today's byte-for-byte scalar pins as a second assertion on parsed values, tracked as an explicit follow-up story rather than implied closed here. **Round 14 SUGGESTION:** `extract_gate_env_key_set`'s doc comment claimed it "scan[s] the whole step rather than assume" `env:` precedes `run:` — the code only ever scanned backward from `run:`; comment corrected to state the actual (and verified fail-closed) behavior, and M2-o's non-empty-result panic message now names a legal `env:`-after-`run:` reorder as a possible cause, not just "missing" or "anchoring logic failed." **Round 15 — CI-caught, not review-caught (real run 31128902318 on `2b2c9bd0`):** 13 of 14 jobs SUCCEEDED, including all three `Test` legs, `Spec Guards`, `Mutation testing`, and `Clippy (ubuntu-latest)`; `Clippy (windows-latest)` failed for real, and `CI Gate` correctly failed as a downstream consequence of that real upstream failure — the first end-to-end evidence, on a genuine failure rather than a synthesized payload, that the gate behaves correctly in production. `Clippy (windows-latest) -D warnings` hard-errored on six dead-code items in `tests/ci_gate_completeness.rs` (`NEEDS_CONTEXT_JOB_KEYS`, `PINNED_ALLOWED_SKIP_IF_EXPRESSIONS`, `all_skip_variants_for`, `build_multi_skip_payload`, `line_declares_job_level_key`, `list_all_ci_yml_job_names`): each is referenced only from the file's three `#[cfg(unix)]`-gated tests (the ones that shell out to bash), which don't exist at all on a Windows build — `#[cfg(unix)]`-gating a TEST does not remove the HELPERS it alone uses from that platform's build; they still compile, now genuinely unused, and `-D warnings` promotes that to a hard error. `Test (windows-latest)` stayed green because the gated tests simply don't exist there to fail — only the lint saw it. Fixed by gating each of the six items with `#[cfg(unix)]` the same way their sole caller already was (matching the pre-existing pattern on `run_check_ci_gate_sh`, which had this right from the start). Thirteen rounds of review, extensive mutation testing, and four-condition RED proofs — all performed on macOS — never surfaced this; **local verification, however rigorous, does not cover the platform matrix.** Verified two ways given no Windows runner was available: (1) `cargo clippy --target x86_64-pc-windows-msvc --tests --all-features -- -D warnings` reaches the C-toolchain build step for `aws-lc-sys` (which needs a real Windows SDK/linker this machine doesn't have) WITHOUT reporting any Rust-level lint error first — the actual dead-code lint pass already completed clean before that unrelated failure; (2) a minimal, structurally-faithful standalone reproduction (six cfg-gated items, one cfg-gated consumer test, no external crates) compiled cleanly via plain `rustc --target x86_64-pc-windows-msvc --test -D warnings --emit=metadata` (exit 0, no linker needed), while the identical reproduction WITHOUT the `#[cfg(unix)]` gates reproduced all six named errors verbatim against the same real target triple — confirming the fix mechanism, not just inspecting source by eye. Also fixed: a stale claim in `extract_gate_step_key_sets`'s neighborhood that cfg-gating a helper is a one-off — the doc comments on each of the six items now say plainly that gating a test also orphans everything only it uses, so the next reviewer checks helpers, not just the test itself, when adding a new `#[cfg(unix)]` test. **Round 16 — close-out: human decision to merge with this residual risk documented, not fixed here (a follow-up story to replace line-based extraction with a real YAML parser is tracked separately).** A CRITICAL was found and independently verified (not merely reported): YAML NODE PROPERTIES — an anchor (`&name`) or a tag (`!tag`/`!!tag`) prefixing a mapping key on the same physical line — defeat every set-equality pin in `tests/ci_gate_completeness.rs`, with zero line breaks involved (orthogonal to round 14's fix, so round 14's guard does not catch it). `&x shell: cat {0}` and `!!str shell: cat {0}`, inserted as a one-line addition to the gate step, are both read by PyYAML AND Ruby Psych as adding a real `shell:` key (`["env","name","run","shell"]`, `shell: "cat {0}"` — the same custom-shell-template override rounds 11/14 already showed defeats the pinned run line) while `extract_key_name_at_indent`'s bare-key branch stops at the space after `&x`/`!!str`, sees no colon, and returns `None` — invisible to `PINNED_GATE_STEP_KEY_SETS` and every other pin built on that one function. Verified directly: one-line insertion, full 17/17 suite green with the malicious line present; `!!str shell: cat {0}` is `actionlint`-clean, `&x shell: cat {0}` makes `actionlint` warn (unused-anchor) but both YAML loaders accept it identically. Independently verified (not taken on report) that the underlying mechanism is not hypothetical: GitHub shipped YAML anchor/alias support in production GitHub Actions on 2025-09-18 ("Actions: YAML anchors and non-public workflow templates," the official github.blog changelog), after four years tracked as `actions/runner#1182` (1,650+ reactions), switching the runner to a YAML-1.2.2-conformant parser — `github/community` discussion #185877 is the FOLLOW-ON request for merge-key (`<<:`) support specifically, which GitHub has NOT shipped, confirming anchors (not merge keys) are the live, shipped mechanism. What remains unverified is this repo's exact payloads' end-to-end behavior against a real runner — not executed against live GitHub Actions infrastructure by anyone on this story, only against PyYAML/Ruby Psych locally. This is the THIRD consecutive round to find a new member of the "lexer disagrees with a real YAML parser" class after the previous member was patched (round 13: comment-indentation truncation, BOM, `? key`; round 14: lone CR and three Unicode line breaks; round 16: node properties) — the finding rate across these rounds is FLAT, not decreasing. **This is documented, not fixed:** the line-break guard's "closed by construction" property is true, and true only, of that one member (line breaks) — it must not be read as covering the lexer layer generally, and nothing in this file currently does. **No automated check in this repository catches a node-property bypass. Code review is the control** — not "mitigated," UNGUARDED, with review as the control. That is acceptable for the specific reason that this residual requires HAND-CRAFTED YAML visible in a diff (`&x shell: cat {0}` / `!!str shell: cat {0}` is not a construction a careless edit, `sed`, or auto-formatter produces) — unlike the pre-S-CIGATE-2 defect this whole story exists to fix, which was silent and already live in production on every push before anyone noticed. The durable fix, tracked as a follow-up story (not opened by this round): parse `ci.yml` once with a real YAML parser (an off-the-shelf Rust crate, or a PyYAML shell-out matching `check-ci-gate.sh`'s own precedent) and assert over the parsed tree, keeping today's byte pins as a second layer on parsed values — round 11's design note correctly rejected hand-rolling a full block-mapping parser, but its conclusion ("hand-roll a narrower slice") conflated that with a different, un-rejected option: using an off-the-shelf parser, the same category of tool `actionlint` and GitHub's own runner (as of 2025-09-18) both use. **S-626-1 research pass (2026-08-10, Q-E) IMPORTANT — `actions/runner`'s public source is not a reliable oracle for server-side workflow *parsing*:** the round-16 finding above cites GitHub shipping anchor support in production on 2025-09-18 as CONFIRM (github.blog changelog), while the same-named file in the public `actions/runner` repo (`YamlObjectReader.cs`, read at `main` on 2026-08-10) still throws `"Anchors are not currently supported"` — proving the runner's public templating-library copy lags at least one shipped parsing feature behind the actual server-side parser that evaluates a pushed `workflow.yml`. Any future argument of the form "the runner source shows X, therefore workflows behave X" must carry this caveat and cannot be taken as CONFIRM on parsing questions from that source alone. This does **not** extend to step-*execution* questions (e.g. step-level `env:` outranking an earlier step's `$GITHUB_ENV` write, confirmed from `StepsRunner.cs`) — execution genuinely happens in the runner binary this source builds, so the runner source remains authoritative there; only workflow-file *parsing* is server-side and out of sync with this public copy. **S-CIGATE-3 (commits `8af710f8`…`130c634f`, 16 commits, 2026-08-11) — the durable fix named at the end of round 16 above is LANDED, not merely re-promised, and has since gone through seven further adversarial-review fix-bursts (`359d6798`…`130c634f`, fix-burst-3 through fix-burst-8) on top of the initial six-pass landing (`8af710f8`…`0e7e570f`) and two documentation commits (`258bbad6`, `80a872e4`) — this paragraph is the final, settled account as of `130c634f`, the last commit in the story.** `tests/ci_gate_completeness.rs` and its shared helper module `tests/common/yaml.rs` no longer build any structural assertion on `str::lines()`/indent arithmetic. `Cargo.toml` adds `saphyr-parser = "=0.0.11"` under `[dev-dependencies]` (exact pin, not a caret range); `tests/common/wf.rs` (~1,299 LOC at initial landing, grown to ~2,483 LOC by `130c634f` through the seven fix-bursts below — the growth is new guard functions and their own regression tests, not scope creep) is a spanned document model built by walking `saphyr-parser`'s low-level `Parser`/`Event` stream — the crate's higher-level `saphyr::Yaml`/`YamlLoader` API is never constructed anywhere in `tests/ci_gate_completeness.rs` or `tests/common/*.rs` (grep-verified: the only two occurrences of the string `saphyr::Yaml` in either file are inside `tests/common/wf.rs`'s own module-doc explanation of why that API is forbidden, not a construction). `tests/common/yaml.rs::extract_job_block` is reimplemented over `WfDoc::parse` with its public signature byte-identical, so all 14 out-of-scope call sites in `tests/backfill_matrix_parity.rs` and `tests/ci_yml_windows_matrix.rs` needed zero changes. The two line-based primitives at the root of the round-13/14/16 defect class — `extract_key_name_at_indent` and `collect_mapping_key_set` — are DELETED from `tests/ci_gate_completeness.rs`, confirmed to have zero remaining callers (every surviving occurrence of either name in the file is inside a `///`/`//` doc comment narrating the pre-S-CIGATE-3 history, not a function call). Test count was unchanged at the initial landing (`EXPECTED_GUARD_TEST_COUNT` stayed pinned at 27) but has since grown: fix-burst-8 (`130c634f`) added two tests closing the "step selected but never runs" gap (`PINNED_ALWAYS_RUN_STEP_KEY_SETS`, see below), moving the pin from 29 → 31; adversarial pass 8 (finding 2) added one further `#[test]` fn, `test_ac_008_guards_are_key_spelling_and_indent_agnostic` — the persisted, standing two-axis (key-spelling × job-body-indent) RED proof AC-008 requires, closing a HIGH drift item (no such proof had survived past the temporary/untracked `ci.yml` copies used to produce it during earlier passes) — moving the pin from 31 → 32, the final, current value, self-checked by `test_this_file_test_count_matches_expected_denominator`. **The round-16 residual (node properties) is now closed structurally, not merely documented — the headline correction to the SCOPE SUMMARY above.** `tests/common/wf.rs::find_key_node_properties` walks the event stream for any mapping key carrying an anchor or tag and is asserted empty against the `ci-gate` job block by a new check (`tests/ci_gate_completeness.rs`, "Assertion 12" / M2-q, AC-007). This closes the gap two independent ways the old line-based checker could not: (1) the key-SET pins (`PINNED_GATE_STEP_KEY_SETS` etc.) now see the smuggled key at all, because the parser resolves `&x shell: cat {0}` / `!!str shell: cat {0}` to a real `shell` key regardless of the node property, where the old `extract_key_name_at_indent` saw the space after `&x`/`!!str`, found no colon, and returned `None`; (2) `find_key_node_properties` separately rejects a node property on a key that is ALREADY a legitimate pinned member (e.g. `&x run: …`) — a case pure key-set comparison can never catch, since the key text itself doesn't change. Both round-16 reproduction forms were RED-proven this session against a temporary, untracked copy of `ci.yml` (the tracked file was never modified): `&x shell: cat {0}` yields step key set `["env","name","run","shell"]` (vs. the pinned `["env","name","run"]`) and `find_key_node_properties` reporting `[{key:"shell", has_anchor:true, tag:None}]`; `!!str shell: cat {0}` yields the identical key-set mismatch and `find_key_node_properties` reporting `[{key:"shell", tag:Some("tag:yaml.org,2002:str")}]`. Also closed by construction, as a side effect of tree-based lookup replacing indent arithmetic: the `POSITIONAL-ASSUMPTION-AXIS` drift item (a hard-coded 4-space job-child indent assumption) and `ADV-P55-MED-002` (flow-style job entries) — neither has an indent literal or line-shape assumption left to defeat, because mapping-child membership is now read from the parse tree, not re-derived from a column count. **What this does NOT close — read alongside, not instead of, the SCOPE SUMMARY above.** (1) **The round-14 non-LF byte scan (`test_ci_yml_contains_no_non_lf_yaml_line_breaks`) is RETAINED, unconditionally, as an independent third layer — it is NOT subsumed by the parser.** `saphyr-parser` implements YAML 1.2, under which NEL (U+0085), LINE SEPARATOR (U+2028), and PARAGRAPH SEPARATOR (U+2029) are correctly NOT line breaks; PyYAML (the tool the round-14 exploit was verified against) implements YAML 1.1, where they ARE. Only the lone-CR (U+000D) case is natively subsumed by the YAML-1.2 parse. Deleting this test on the assumption "we have a real parser now" would silently reopen two-thirds of the round-14 gap — it is why AC-005 exists as its own named acceptance criterion in the S-CIGATE-3 story rather than being left implicit. (2) **Byte-for-byte scalar pins are retained as a second layer, not replaced — now sourced from `Event::Scalar` values, with an added style assertion that is itself load-bearing.** The real parser resolves `if: ${{ always() }}`, `if: "${{ always() }}"`, and `if: '${{ always() }}'` to IDENTICAL scalar text, differing only in `ScalarStyle`. The pre-parser byte-comparison implicitly rejected the two re-quoted forms (the literal quote characters never matched an unquoted pin); to preserve that exact strictness rather than silently loosen it, every rewritten pin additionally asserts `style == ScalarStyle::Plain` (`extract_and_normalize_if_expr`, `extract_and_normalize_sole_run_line`, and siblings in `tests/ci_gate_completeness.rs`) and hard-errors on any other style or a YAML tag on the value. Without this style check, the migration would have quietly accepted re-quoted forms the old checker rejected — a real, if easy to miss, loosening. (3) **Review scope is unchanged in kind, wider in files** — see the SCOPE SUMMARY "Review scope" line above, now four files, not three. (4) **`scripts/check-ci-gate.sh`'s runtime `toJSON(needs)` evaluation is untouched by this story** — it never parsed `ci.yml`; S-CIGATE-3 is scoped entirely to the static, test-time assertions in `tests/ci_gate_completeness.rs`/`tests/common/wf.rs`. (5) **The sudo bound and the two unpinned `uses:` values on the `ci-gate` decision path (round 13's `$GITHUB_ENV` finding / the S-626-1 `$GITHUB_PATH` research pass above) are unchanged** — still out of scope, still the documented residual; a real YAML parser has no bearing on what a `uses:` action does at runtime. **On the dependency itself:** `saphyr-parser` is a `[dev-dependencies]`-only crate — it never enters the `lib`/`bin` compile graph the `msrv` CI job checks (that job's `cargo check --all-features --locked` is deliberately scoped to lib+bins, NOT `--all-targets`, specifically because dev-dependencies like `wiremock` don't build at 1.85.0 — see the `msrv:` job's own comment in `ci.yml`). **Correction to the S-CIGATE-3 story's own AC-006 rationale (the story is wrong here, not this record):** the story justifies the `=0.0.11` exact pin as protecting the `msrv` job from an unreviewed transitive bump — that specific mechanism does not apply, since dev-dependencies sit outside the `msrv` job's `cargo check` scope entirely; a version bump here cannot break that job regardless of pin strictness. The pin's actual justification, independent of MSRV: `0.0.x` carries no semver guarantee by convention (a `0.0.x`→`0.0.y` bump may be breaking), so `=0.0.11` forces any version change through a reviewed `Cargo.toml` diff rather than a silent `cargo update`. Separately, and for context only (not the pin's rationale): `saphyr-parser` 0.0.11's own MSRV is exactly 1.85.0 against this repo's `rust-version = "1.85"` — zero headroom — but this is NOT enforced by the `msrv` job for a dev-dependency; it would only bite a future promotion of this crate out of `tests/` into `src/`. **Fix-bursts 3–8 (`359d6798`…`130c634f`) — what the seven follow-on adversarial-review rounds actually closed, beyond the initial landing above.** Six fresh-context adversarial passes ran against the newly-landed parser-based module and found real gaps in it — the same "second reviewer, different context" discipline this whole file's history is built on, now applied to the replacement machinery itself rather than to `ci.yml`. What is newly TRUE as of `130c634f`, each traceable to a specific guard: (1) **duplicate mapping keys are refused at every mapping-child lookup**, not merely tolerated as "the caller's problem" — `find_unique_entry` (`tests/common/wf.rs`, fix-burst-3, ADV-SC3-P1-MED-004) panics rather than silently returning the first match, and every lookup function in the module (steps, job body, step body, nested `env:`/`with:` children) was converted to route through it; `assert_no_duplicate_keys` separately rejects a duplicate **job id** under `jobs:` (`WfDoc::parse (jobs:)` call site) and a duplicate key inside a fully-resolved key set a caller hands back to ITS OWN caller (e.g. two `CARGO_TERM_COLOR:` children of one `env:` block). Fix-burst-8 (ADV-SC3-P6-LOW-001) closed the one lookup that had been missed by the fix-burst-3 sweep: `Job::value_of`/`Step::value_of` were still first-match-silent; both now panic identically. (2) **Multi-document YAML streams are rejected**, not silently narrowed to the first document — `assert_single_document` (fix-burst-4, ADV-SC3-P2-*) asserts at most one `Event::DocumentStart` in the parsed stream; a `---`-separated second document in `ci.yml` now fails loudly instead of being invisibly ignored by every downstream lookup. (3) **Complete job-level key-set pins now exist for all seven always-run `ci-gate.needs` members** (`fmt`, `clippy`, `test`, `msrv`, `deny`, `spec-guard`, `check-signing-workflow-injection`) via `PINNED_ALWAYS_RUN_JOB_KEY_SETS` (fix-burst-8, ADV-SC3-P6-MED-001) — previously only `ci-gate` itself had a job-key-set pin, so a smuggled job-level key (e.g. `continue-on-error:`, `defaults:`) on any OTHER always-run job was invisible. (4) **Complete, ordered per-step key-set pins now exist for the five non-matrix always-run jobs** (`msrv`, `fmt`, `deny`, `check-signing-workflow-injection`, `spec-guard`) via `PINNED_ALWAYS_RUN_STEP_KEY_SETS`, plus a dedicated by-value pin for `clippy`'s matrix-job lint-gate step (`PINNED_CLIPPY_GUARD_STEP_KEYS`) and the pre-existing `test`-job POL-11 guard-step pins — closing the "the right step is selected but an `if: false` or `shell: cat {0}` on it means it never actually runs" class, which a selection-only guard (checks the step exists and has the right name) cannot see by construction. (5) **`PINNED_WORKFLOW_ROOT_KEYS`** (`&["env", "jobs", "name", "on"]`) pins the workflow's own root-level key set, closing the same "unpinned open set under a higher-level pin" shape as round 12's `PINNED_GATE_ENV_KEYS`/`PINNED_WORKFLOW_ENV_KEYS` finding, one level further out. (6) **Every migrated byte pin additionally asserts `ScalarStyle::Plain`** (documented above as part of the initial landing, reconfirmed unchanged through fix-bursts 3–8 — no fix-burst touched this mechanism). (7) **`tests/common/wf.rs` now carries its own `#[cfg(test)] mod tests` (fix-burst-6, ADV-P60-HIGH-001 parity fix) with its own `EXPECTED_WF_TEST_COUNT` tripwire, currently pinned at 26** — before this, the sibling module's `EXPECTED_GUARD_TEST_COUNT`/`#[cfg(...)]`-allowlist enforcement (originally built for `ci_gate_completeness.rs` alone) had no equivalent for `wf.rs` itself, so a test inside `wf.rs` could be silently `#[ignore]`d or `#[cfg(...)]`-gated off with no counter to catch it; `wf.rs`'s allowlist is empty (stricter than the sibling's `#[cfg(unix)]` allowance), since `wf.rs` has no platform-gated test today. All seven items above are independently re-derivable from the cited constants/functions in `tests/common/wf.rs` and `tests/ci_gate_completeness.rs` as of `130c634f` — none is inferred from a commit message alone.
- **Errors:** Always suggest what to do next. Map to exit codes via `JrError::exit_code()`
- **Output:** `--output json` returns structured JSON for both success and errors. Human text is default.
- **JSON render invariant (#526):** Every `--output json` path in `src/cli/` MUST route through `output::render_json` or `output::print_output` (which delegates to `render_json`). Direct `serde_json::to_string_pretty` calls and compact `serde_json::json!` Display printing are both forbidden. Output is pretty-printed. As of v0.6+ (#526), `jr issue create --request-type` and `jr project fields` changed from compact to pretty-printed JSON; `jq` and programmatic parsers are unaffected.
- **Non-interactive:** `--no-input` disables prompts (auto-enabled when stdin is not a TTY). Commands must have fully non-interactive flag equivalents.
- **Idempotent:** State-changing commands (move, assign) exit 0 if already in target state.
- **Tests:** TDD. Unit tests inline, integration tests in `tests/`. Property tests with proptest. Snapshot tests with insta.
- **No unsafe code** without explicit justification in a comment.
- **No lint suppression without refactoring.** If clippy warns (e.g., `too_many_arguments`), refactor to fix the root cause — don't add `#[allow]`. If refactoring is impractical, ask the user before suppressing and include a justification comment.
- **No let-chains.** `if let … && …` / `… && let …` syntax requires Rust ≥1.88 with edition 2024; MSRV is 1.85. Use nested `if` blocks instead. The `msrv` CI job catches violations in lib + bins; inline `#[cfg(test)]` modules in `src/` and integration tests in `tests/` are both outside its enforceable scope (see the `ci.yml` msrv scope comment). Temporary — delete this entry and the three citing in-code comments when MSRV is raised to ≥1.88.
- **Default to fixing code, not tests.** When a test fails, assume the test is correct and fix the implementation using idiomatic Rust. Only modify a test when requirements have changed — not to accommodate non-idiomatic code or lint workarounds.
- **Test naming:** New tests use `test_<verb>_<subject>_<expected_outcome>` (e.g., `test_auth_switch_returns_json_ok`). Existing tests are NOT renamed for style alone — but a name asserting a guarantee its body doesn't check is a defect, not a style deviation, and may be corrected independently. See `docs/specs/test-naming-convention.md`.
- `--dry-run` is implemented on `issue edit` (multi-key positional + `--jql`-resolved sets) with `--output json` support. Pre-PR2 NFR-O-C originally documented this as DOCUMENT-AS-IS-OUT-OF-SCOPE; superseded by issue #110 part 2.
- `jr version --output json` is not implemented (NFR-O-X: deferred to v2; consider for `release-notes` automation).
- `sprint list` table omits start/end dates (NFR-O-U: deferred UX pass v2; available in API response).
- `auth status` has no `--output json` support (NFR-O-N: deferred; neither single-profile nor multi-profile JSON is implemented — `src/cli/auth/status.rs::status()` writes human text only via `println!`). JSON path planned alongside future `auth list --output json` extension.
- JSON output has no `_meta: {version: N}` envelope (NFR-O-P: deliberate for v0.5; consider for v2 to enable downstream-parser schema-drift detection).

### Output channels

CLI handlers follow one of five output-channel profiles:

1. **Pure** — stdout only (JSON or table data); no stderr output at all.
2. **Read-only** — stdout for data, stderr for hints/warnings (e.g., truncation notices, "showing N of M").
3. **Mixed** — stdout for success data, stderr for errors and hints; applies to most read commands.
4. **Symmetric** — stdout for `--output json`, stderr for human-readable errors in either mode; state-changing commands that also print a result use this profile.
5. **No-log facade** — state-changing commands that emit only a JSON result on stdout (e.g., `{"key": "FOO-123"}`); no progress or logging to stderr.

The distinction matters for scripting: pipe stdout for data, redirect stderr for diagnostics. Never write diagnostic text to stdout in profiles 1/2/3/5.

## Key Decisions

See `docs/adr/` for detailed rationale:
- ADR-0001: Thin client vs generated API client
- ADR-0002: OAuth 2.0 with embedded secret (superseded — see ADR-0006)
- ADR-0003: reqwest with rustls-tls
- ADR-0004: Per-feature specs, not a growing master spec
- ADR-0005: GraphQL hostNames for org discovery (team support)
- ADR-0006: Embedded `jr` OAuth app with compile-time XOR obfuscation (re-supersedes ADR-0002)
- ADR-0007: Multi-profile fields bug — per-profile field IDs must be read from ProfileConfig, not global config
- ADR-0008: Asset enrichment key correctness — HashMap key must be (workspace_id, object_id), not object_id alone
- ADR-0009: handle_open uses instance_url(), not base_url() — base_url() is API-only; browser URLs must use instance_url()
- ADR-0010: list_worklogs pagination loop — single-page fetch silently truncates; must use offset pagination
- ADR-0011: Type-level Profile fence deferred — convention-based soft fence is sufficient for current team size (v0.5.x)
- ADR-0012: Module shard rule — src/cli/ files at ≥1,000 LOC are shard candidates; exceptions: adf.rs, api/auth.rs
- ADR-0013: PKCE deferral — Atlassian 3LO does not support public-client PKCE as of 2026-05; reactivation trigger defined
- ADR-0014: JSM request-type dispatch fork in `jr issue create` (`--request-type` routes to `/rest/servicedeskapi/request`)
- ADR-0015: Proactive resolution enforcement on done-category transitions (`jr issue move` requires `--resolution` or `--no-resolution`)
- ADR-0016: Windows build target (x86_64-pc-windows-msvc, AppData config/cache paths, Windows Credential Manager keyring, .zip packaging, CI)

## Specs & Plans

- **v1 design spec:** `docs/superpowers/specs/2026-03-21-jr-jira-cli-design.md`
- **v1 implementation plan:** `docs/superpowers/plans/2026-03-21-jr-implementation.md`
- **Feature specs (post-v1):** `docs/specs/` — one spec per feature, read before implementing

When adding a new feature:
1. Read this file
2. Read the v1 design spec for architectural context
3. Read relevant ADRs
4. Create a feature spec in `docs/specs/` before implementing
5. Follow TDD — write tests first

## Gotchas

- **`rust-toolchain.toml` outranks `rustup default` — msrv job requires `RUSTUP_TOOLCHAIN` env (S-626-1):** When `rust-toolchain.toml` is present in the working directory (as it is in this repo, with `channel = "stable"`), it outranks `rustup default` in rustup's precedence chain. Rustup precedence, highest to lowest: `+toolchain` > `RUSTUP_TOOLCHAIN` env > directory override (`rustup override set`) > `rust-toolchain.toml` > `rustup default`. (Note: directory overrides and `rust-toolchain.toml` are additionally ranked by proximity to cwd — a nearer file beats a farther override — but this is immaterial in CI where no directory override exists.) The pre-fix `msrv` CI job pointed at the tip of dtolnay's `1.85.0` version branch, which hard-codes the toolchain version and has no `toolchain` input declaration. The action correctly installed 1.85.0 and set it as `rustup default`. `cargo check` then ran in the repo root where `rust-toolchain.toml` (`channel = "stable"`) outranked `rustup default`, so the check silently ran under stable — a false-green. Because the old SHA had no `toolchain` input, adding `with: {toolchain: "1.85.0"}` alone would have been silently ignored by GitHub Actions (at most an "Unexpected input(s)" warning); the SHA replacement to `fa04a1451ff1842e2626ccb99004d0195b455a88` was a hard prerequisite — this version of the action declares `toolchain` as a required input. The fix adds both `with: {toolchain: "1.85.0"}` (to install the correct toolchain) AND `env: {RUSTUP_TOOLCHAIN: "1.85.0"}` on the `cargo check` step — `RUSTUP_TOOLCHAIN` outranks `rust-toolchain.toml`, ensuring cargo uses 1.85.0 at check time. Both are load-bearing — the action input installs the toolchain; the env override ensures `cargo check` uses it. Omitting the env override silently reverts to stable validation. Note: at the currently pinned SHA (`fa04a1451ff1842e2626ccb99004d0195b455a88`), the action validates `toolchain:` as a hard-required input — dropping the `with:` block fails the job loudly. The only genuinely silent revert vector is removing the `RUSTUP_TOOLCHAIN` env override.
- **Windows config/cache paths (BC-6.1.014, BC-6.2.016):** On Windows, `jr` uses idiomatic AppData locations — config at `%APPDATA%\jr` (Roaming, via `dirs::config_dir()`) and cache at `%LOCALAPPDATA%\jr` (Local, via `dirs::cache_dir()`). Unix paths (`~/.config/jr`, `~/.cache/jr/v1/<profile>/`) are unchanged. The `JR_CONFIG_DIR`/`JR_CACHE_DIR` debug seam (Decision 5, ADR-0016) provides cross-platform test isolation; `XDG_CONFIG_HOME`/`XDG_CACHE_HOME` remain Unix-only.
- **Windows Credential Manager isolation (SEC-WCM-DOC):** On Windows, `jr` stores OAuth tokens and API tokens via the `keyring` crate's `windows-native` feature, which uses Windows Credential Manager (`CRED_TYPE_GENERIC` entries). Secrets stored there are accessible to any process running in the same user session — the same security posture as `gh` and `git-credential-manager`. OS-level user-session isolation is the trust boundary on Windows. Do not use `jr` in a shared-session environment (e.g., a shared Windows terminal server) without understanding this posture. **DPAPI-encrypted-file fallback for oversized OAuth tokens (ADR-0021, S-cycle4-dpapi-storage-fix, issue #759):** Windows Credential Manager caps a single credential blob at ~2560 bytes, which some Atlassian OAuth refresh tokens exceed. When `store_oauth_tokens` hits that ceiling (`keyring::Error::TooLong`), the whole access/refresh pair falls back to a user-scope, DPAPI-protected file at `%LOCALAPPDATA%\jr\secrets\<profile>\oauth-tokens.dat` instead of failing the login — the same OS-level user-session trust boundary applies (DPAPI-protect is user-scoped, `CRYPTPROTECT_UI_FORBIDDEN`, never machine-scope). The pair is always stored entirely in one backend (never split across keyring and DPAPI-file), and a pre-existing keyring pair is deleted before the DPAPI write so a crash mid-fallback can't leave a stale pair shadowing the fresh one. This fallback is engaged only past the keyring's size ceiling; a token that fits is stored in Windows Credential Manager exactly as described above. **Verification status (DEC-335):** the routing/path/envelope logic is implemented and unit-tested cross-platform, but the actual `CryptProtectData`/`CryptUnprotectData` FFI round-trip cannot be exercised on a non-Windows development host — per `src/api/auth_windows_store.rs`'s module header, real-Windows verification of that round-trip is a pending F4 CI/manual-smoke-test item, not yet a proven on-every-PR fact.
- **jr.exe requires an explicit 8 MB main-thread stack on Windows (WIN-STACK):** Windows PE headers default to a 1 MB main-thread stack, while Linux/macOS default to 8 MB. The `#[tokio::main]` async runtime + clap dispatch + result rendering exceed 1 MB, causing jr.exe to crash on normal commands like `jr issue list`. The fix is embedded at compile time via `.cargo/config.toml` under `[target.x86_64-pc-windows-msvc]`: `rustflags = ["-C", "link-arg=/STACK:8388608"]` — this sets an 8 MB reserve in the PE header. Unix builds are unaffected. `RUST_MIN_STACK` is NOT a valid fix: it only affects `std::thread::spawn` threads (test-harness workers), not a process's main thread.
- **Multi-profile boundary:** every cache reader/writer takes `profile: &str` as its first arg. Pass `&config.active_profile_name` from any handler that has `&Config` in scope. Cross-profile cache leakage is a correctness bug, not a UX issue — sandbox vs prod custom-field IDs can differ.
- **Per-profile vs shared keychain keys (S-cycle3-percred-storage, BC-1.4.031):** `email` and `api-token` are namespaced per-profile as `<profile>:email` / `<profile>:api-token` (`store_api_token`/`load_api_token`, `src/api/auth.rs`), symmetric with the existing `<profile>:oauth-access-token` / `<profile>:oauth-refresh-token` namespacing — both are cloudId/profile-scoped, not account-level. Only `oauth_client_id`/`oauth_client_secret` remain flat keychain keys (account-level, shared across profiles). There is deliberately no legacy-flat-key fallback for `email`/`api-token` on any profile, including `"default"` — that's a difference from the OAuth keys, whose `"default"` profile still lazy-migrates legacy flat keys on first read. **Wired up (S-cycle3-remove-logout-semantics, BC-1.2.013/BC-1.2.014):** `auth remove` (`src/api/auth.rs::clear_profile_creds`) now clears BOTH the OAuth pair and the namespaced `<profile>:email`/`<profile>:api-token` pair. `auth logout` (`src/cli/auth/logout.rs::handle_logout`) intentionally does NOT clear the api-token pair — it clears only the OAuth pair via `clear_profile_oauth_pair`, staying non-destructive for API-token profiles. **On an api-token profile specifically (DEC-322), `auth logout` no longer silently no-ops** — it prints an informational, non-error stderr notice (`"This profile uses API-token auth — nothing to log out; use `jr auth remove <profile>` to delete stored credentials."`) and exits 0; the api-token pair itself is left completely untouched. This notice is stderr-only and never appears on stdout in any output mode, including `--output json` (the JSON payload shape is unchanged from the pre-DEC-322 no-op behavior). **`auth refresh` clears nothing before re-obtaining credentials (S-cycle3-chosen-flow-reconcile, BC-1.2.051 Invariant 2, I-6, "relogin-then-replace"):** `src/cli/auth/refresh.rs::refresh_credentials` no longer calls any clear function up front — it calls `login_token`/`login_oauth` directly, which resolve/obtain the new credential value FIRST and only then persist it via `auth::store_api_token`/`auth::store_oauth_tokens` (an unconditional overwrite of the existing keychain entry). A refresh that fails to obtain a usable replacement therefore leaves the profile's existing credentials completely untouched. This replaced the prior "clear-then-login" sequence, which called `clear_profile_oauth_pair` (OAuth) before login — see the CHANGELOG `[Unreleased]` entry for DEC-321 for the full before/after.
- **`--profile` is rejected on `auth switch` (use the positional)** (S-663-1, BC-1.2.047, issue #663, breaking change): `jr auth switch --profile <X> <NAME>` exits 64 — the global `--profile` flag has no effect on this subcommand, only the positional `<NAME>` selects the switch target. `List`/`Remove`/`Login`/`Status`/`Refresh`/`Logout` are unaffected and continue to honor `--profile`.
- **Cache format changes:** `~/.cache/jr/v1/<profile>/cmdb_fields.json` stores `(id, name)` tuples. Old format (ID-only) causes deserialization failure, handled as cache miss. If you change cache structs, old caches auto-expire (7-day TTL) or fail gracefully. To break compatibility cleanly, bump the cache root from `v1/` to `v2/` — old files orphan harmlessly.
  - **Request-type caches (S-288-pr2):** `request_types_<sid>.json` + `request_type_fields_<sid>_<rtId>.json`; shape from `src/types/jsm/request_type.rs`. Same 7-day TTL / `v1/`-bump rules; deserialize failure → cache miss → refetch (self-heals).
- **`jr requesttype fields <NAME|ID>` numeric-bypass edge case:** all-ASCII-digit input is treated as a numeric RT ID and skips `partial_match`. A request type *named* `"100"` is therefore unreachable by name (no escape exists; look up its ID via `jr requesttype list --output json | jq`). Tracked behavior, not a bug — relevant if you add a `--by-id` flag.
- **Cache-write error handling — two models (S-288-pr2):** most writers in `src/cache.rs` propagate via `?`; the request-type writers (`write_request_type_cache`, `write_request_type_fields_cache`) swallow + `eprintln!("warning:…")` so a failed write never breaks a successful API call. New cache family → pick (a) propagate if correctness-critical, (b) swallow+warn if pure read-acceleration; document the choice in the writer's rustdoc.
  - **`write_cmdb_fields_cache` (`src/cache.rs`, S-525/CR-007):** model-b writer — swallows disk-write errors with `eprintln!("warning: failed to write cmdb_fields cache: {e}")` and returns `Ok(())`. Its call site in `src/api/assets/linked.rs` uses `.ok()` to discard the infallible result. Do NOT re-introduce `let _ =` or `?` at the call site.
  - **`write_object_type_attr_cache` (`src/cache.rs`, S-525/CR-007):** model-b writer — swallows disk-write errors with `eprintln!("warning: failed to write object_type_attrs cache: {e}")` and returns `Ok(())`. Its call site in `src/api/assets/objects.rs` uses `.ok()` to discard the infallible result. Do NOT re-introduce `let _ =` or `?` at the call site.
- **`aqlFunction()` not `assetsQuery()`:** The Jira Assets JQL function is `aqlFunction()`. It requires the human-readable field **name**, not `cf[ID]` or `customfield_NNNNN`. AQL attribute for object key is `Key` (not `objectKey` — that's the JSON field name).
- **Status category colors are fixed:** `green` = Done, `yellow` = In Progress, `blue-gray` = To Do. These mappings are hardcoded in Jira Cloud across all instances. Used by `--open` filtering.
- **Embedded OAuth app uses fixed callback port 53682.** Callback URL is `http://127.0.0.1:53682/callback` — literal `127.0.0.1` (not `localhost`) to force IPv4 and dodge the macOS/Chrome `localhost`→`::1` pitfall; must match the Atlassian Developer Console registration, so changing the port is a breaking release. Release builds inject `JR_BUILD_OAUTH_CLIENT_ID`/`_SECRET` via `build.rs` → XOR-obfuscated `embedded_oauth.rs`. BYO sources (flag/env/keychain) keep dynamic-port behavior. Detail: ADR-0006, `docs/superpowers/specs/2026-04-30-embedded-oauth-app-design.md`.
- **`src/api/auth_embedded.rs` is a thin sibling module** to `auth.rs`. Keep
  obfuscation plumbing there; keep keychain/OAuth flow plumbing in `auth.rs`.
- **`--verbose` is header-only (SD-003 breaking change):** As of v0.6, `--verbose` shows method + URL only. It does NOT print request/response bodies. To inspect bodies (e.g., for debugging API calls), use `--verbose-bodies`. This flag emits a 3-line PII warning to stderr because bodies contain accountIds, email addresses, and ADF content. Do not use `--verbose-bodies` in shared terminals, debug log files piped to shared storage, or AI-agent context windows. Migration: `jr ... --verbose` → `jr ... --verbose --verbose-bodies` if body inspection was relied upon.
- **`refresh_oauth_token` resolves credentials internally** (keychain →
  embedded) — callers pass only `profile`. Do not re-introduce
  `client_id`/`client_secret` parameters; they short-circuit the resolver.
- **`--open` filter uses two mechanisms:** Jira issues → `statusCategory != Done` injected into JQL (server-side). Connected CMDB tickets (`cli/assets/tickets.rs::filter_tickets`) → client-side `status.colorName != "green"`, because CMDB tickets don't support JQL `statusCategory` filtering.
- **User pagination advances by `USER_PAGE_SIZE`, not returned count:** In
  `src/api/jira/users.rs`, both `search_users_all` and `search_assignable_users_by_project_all`
  increment `start_at` by `USER_PAGE_SIZE` (100) after each page, NOT by the number of
  users returned. This is a workaround for JRACLOUD-71293: Jira uses fixed-window
  pagination (selects range [startAt, startAt+maxResults) then applies permission
  filtering), so a short non-empty page does NOT mean end-of-data. Advancing by returned
  count would overlap windows and produce duplicates. Do not change this behavior.
- **`jr requesttype list/fields` (JSM, `cli/requesttype.rs`):** `list` (Name, Description) + `fields <NAME|ID>` (Field Name, Required, Type). Caches per `(profile, sid[, rtId])`; `--search` results are never cached, only the full list. Cache fns: `{read,write}_request_type[_fields]_cache`.
- **`require_service_desk` call-site label (BC-X.8.004):** `call_site_label: &'static str` is embedded before ` a Jira Service Management project.` (verb dropped for plural/singular agreement), so every caller must pass a full noun-phrase ending in the matching verb (e.g. `…require` / `…requires`). Canonical contract + current caller list: `src/api/jsm/servicedesks.rs::require_service_desk` rustdoc.
- **`jr issue create --request-type` dispatch fork (S-288-pr4):** `--request-type` set → `handle_create` short-circuits to `handle_jsm_create`, POSTing `/rest/servicedeskapi/request` instead of `/rest/api/3/issue` (gated solely on `request_type.is_some()`; absent → platform path byte-for-byte unchanged, **except for the DEC-188 pre-flight guards below**). On JSM path: `--type` silently ignored (BC-3.8.010), custom fields via `--field`, 401 → `write:servicedesk-request` scope hint. Detail: ADR-0014.
- **`jr issue create --on-behalf-of` without `--request-type` exits 64 pre-flight (S-639-1, DEC-188, BC-3.8.013, breaking change, v0.6.0-dev.12); `--field`'s half of this guard was REVERSED by S-578-4 (DEC-310, registered 2026-08-26, #578) — see the next entry.** `handle_create` (`src/cli/issue/create.rs`) checks, immediately after the JSM dispatch-fork `if request_type.is_some()` block and BEFORE project-key resolution, interactive prompts, the `--description-stdin` blocking read, or any HTTP call: `on_behalf_of.is_some()` → exit 64 `JrError::UserError`. Empty values (`--on-behalf-of ""`) still trip the guard. `--output json` gets the same `{"error": "…", "code": 64}` envelope as any other exit-64 UserError. MUST NOT be implemented via `#[arg(requires = "request_type")]` — that yields clap exit 2, not the required exit-64 `JrError::UserError`. Migration: add `--request-type <NAME>` or drop the flag. Detail: `docs/specs/issue-create-preflight-guards.md` (historical; describes the now-superseded combined-guard shape for `--field`).
- **`jr issue create --field NAME[:kind]=VALUE` on the platform (non-JSM) path resolves via `createmeta` instead of exiting 64 (S-578-4, BC-3.3.010/BC-3.3.011, DEC-310, #578):** Reverses S-639-1/DEC-188's `--field`-alone and combined pre-flight guards — those are REMOVED. After step 2 (`--on-behalf-of`, above) and step 2a (`parse_field_kv` hint-syntax parse, malformed hint exits 64), a new step 2b ten-member dedicated-flag × `--field` collision guard (`field_resolve::detect_flag_field_overlap`, `field_resolve::CREATE_D2_GOVERNED_KEYS`) rejects a `--field` pair whose wire key matches a dedicated flag (`summary`/`description`/`issuetype`/`priority`/`components`/`labels`/`parent`/`assignee` as static key compares, plus `--points`/`--team` via RESOLVED-ID equality against the configured `story_points_field_id`/`team_field_id` — config-only, never HTTP) — exit 64, zero HTTP. `labels` IS governed here (unlike `issue edit --field`'s Gate B, which excludes it for BUG-LABEL-400 reasons that don't apply on create — one code path writes `fields["labels"]` unconditionally). After project/type resolution (step 3) and any blocking `--description-stdin` read (step 4a), step 4b resolves each surviving `--field` pair against `get_issue_types_for_project` (S-331, name→id) + `get_createmeta_fields` (S-580-1, `GET .../createmeta/{proj}/issuetypes/{itid}`, offset-paginated) — the SAME `customfield_NNNNN` bypass, cache-first `fields.json` name resolution, and hint-kind (`:option`/`:id`/`:name`/`:asset`) dispatch `issue edit --field` uses (`field_resolve::resolve_edit_fields`'s `FieldMetaSource::Create` arm, sharing the `dispatch_field_value` helper with the `Edit` arm — no second, independently-implemented dispatch). All-or-nothing: any resolution failure exits 64 with zero POST. Resolved fields appear in the create-path table-mode success echo, interleaved alphabetically with `--summary`/`--priority`/etc; JSON mode is unaffected (no `changed_fields` key). Documented non-firing residual: `--points 5 --field "Story Points"=8` (a display-name spelling, not the `customfield_NNNNN` bypass form) does NOT trip the D2 guard — resolving a display name would require hoisting general field-name resolution ahead of the guard's zero-HTTP boundary.
- **When changing `DEFAULT_OAUTH_SCOPES`:** (1) update the embedded `jr` OAuth app's
  permissions in the Atlassian Developer Console at
  https://developer.atlassian.com/console/myapps/ before tagging the release, and
  (2) add a CHANGELOG entry mentioning the re-consent prompt so users aren't surprised.
  Existing access tokens continue working with old scopes until expiry; new logins and
  refresh-token mints trigger re-consent.
- **`board view` truncation hint emits to stderr:** The truncation hint ("Showing N of M
  columns — use --all to see everything") is written to stderr, consistent with the
  convention used by `issue list` and `sprint current`. This is intentional — stderr
  keeps hints out of `--output json` and pipe-friendly stdout.
- **Atlassian's expired-token 401 has no machine-readable signal:** body is `{"errorMessages":[…expired…]}` with NO `code` field and NO RFC-6750 `WWW-Authenticate` header. Auto-refresh (S-3.03) therefore triggers on blanket-401 (`gh` CLI pattern), not substring-match or header inspection. Detail: `.factory/research/S-3.03-wave3-verification.md` (Claim 2, REFUTED).
- **Refresh tokens are strictly single-use on Atlassian.** The "10-minute reuse window" from secondary sources is undocumented and fails in clusters — treat each as one-shot. Concurrent refresh MUST go through `src/api/refresh_coordinator.rs` per-profile single-flight to avoid `invalid_grant` races. Detail: `.factory/research/S-3.03-v2-design-verification.md` (Claim 5, REFUTED).
- **`refresh_coordinator.rs` mutex layering:** outer `std::sync::Mutex<HashMap>` held only briefly for lookup/insert, released BEFORE any `.await`. Inner `tokio::sync::Mutex<RefreshState>` held across the refresh `.await` — MUST be `tokio::sync::Mutex` (no poison-on-panic; a panicked refresh must not permanently break the coordinator). Detail: S-3.03 v2 spec.
- **Bulk transitions are not idempotent.** Single-key `move` exits 0 if already in target status; multi-key `move` (positional or `--to`) transitions every key unconditionally → expect per-key 400s on workflows that reject same-status moves. Pre-filter with `jr issue list --jql "… AND status != \"<target>\""`. Note: `--jql` selection is on `edit` only, not `move`.
  - **Bulk transition wire schema is NESTED, not flat (FIX-BULK-TRANSITION-001):** `POST /rest/api/3/bulk/issues/transition` requires `{"bulkTransitionInputs":[{"selectedIssueIdsOrKeys":[…],"transitionId":"…"}],"sendBulkNotification":false}`. The flat top-level shape (`selectedIssueIdsOrKeys` + `transitionId` at root) documented in the Atlassian OpenAPI JSON is wrong — live Jira rejects it with 400 "bulkTransitionInputs must not be empty". Same asymmetry class as `labelsFields`/`"labels"` and `issueType`/`"issuetype"`. Confirmed: Atlassian community 2026-02-19 + live run 27156639337.
- **`/rest/api/3/search/jql` repeated-`nextPageToken` = JRACLOUD-95368** (live-data drift between page fetches), NOT -94632/-92049/-85546 — those three are misattributed (verified, issue #361/PR #364). `search_issues` / `search_issue_keys` in `src/api/jira/issues.rs` carry an anti-loop guard (sets `has_more=true` on abort) plus an incremental `seen_keys: HashSet` that dedupes all exit paths in first-occurrence order. Two load-bearing string rules: (1) the stderr ORDER BY hint must read "append `, key ASC` to an existing sort, or use `ORDER BY key ASC` if none" — never a bare "add `ORDER BY key ASC`" (JQL allows one ORDER BY → HTTP 400 if user already sorts); (2) the literal `"JRACLOUD-95368"` is pinned by `tests/rate_limit_cap_tests.rs` and `tests/search_issue_keys.rs::test_..._emits_jracloud_95368_literal` — update together.
- **`issue edit` description echo asymmetry (issue #398):** Table/human output echoes
  `description → (updated)` — a marker, never the content. JSON `changed_fields.description`
  carries the **raw user-supplied input string** from `--description` / `--description-stdin`,
  not `"(updated)"` and not an ADF→text round-trip. The two channels intentionally differ:
  the human channel optimizes for scannability; the machine channel must be lossless. Do NOT
  "fix" them to match — this asymmetry is load-bearing. Tested by VP-398-002
  (`test_bc_3_4_012_description_echo_is_updated_marker_not_content` and
  `test_bc_3_4_013_description_echo_is_raw_input_string_not_marker`).
- **`issue edit --field` constraints and JSM behavior (issue #396):**
  (1) single-key only — C-1 guard exits 64 on bulk.
  (2) Changing a JSM issue's **Request Type** is unsupported by any Jira Cloud API — `--field` rejects `sd-customerrequesttype` (JSDCLOUD-4609; PUT 500; out-of-scope).
  (3) JSM Urgency/Impact and other RT select fields CAN be set via `--field NAME=VALUE` **only if** an admin has added the field to the issue's agent Edit screen (portal-only by default).
  (4) `--field` adds one `GET …/editmeta` round-trip (skipped when absent) to validate field + resolve `allowedValues`.
  (5) `write_fields_cache` (in `resolve_edit_fields`) is unconditional on miss/stale (mirrors `write_cmdb_fields_cache`); cache-touching tests use `jr_cmd_with_xdg` + per-test `TempDir` to avoid the real `fields.json`.
  (6) `--field` + `--label` on one key → exit 64 (mutual-exclusion block in `edit.rs::handle_edit`); without the guard the label→bulk routing fork would silently drop the `--field` write. Combined label+custom-field bulk tracked at #331. [FIX-F5-001]
- **`issue edit --label` endpoint fork (BUG-LABEL-400):** `handle_edit_bulk_labels` routes on key count. ONE key (incl. a `--jql` set matching one) → `PUT /issue/{key}` with **bare-string** labels via `update_issue_labels` (sync 204). TWO+ keys → `POST /bulk/issues/fields` with `{"name":…}` **objects** via `build_labels_edited_fields` (async poll). The two payload shapes are **asymmetric — do not unify**; both were proven against real Jira (single-key path + #446 bulk-schema fix after live 400s). Broader schema-verification effort: #331.
- **`issue edit --type` multi-key bulk path (S-331, BC-3.4.018/019):**
  1. **camelCase/lowercase asymmetry (load-bearing, do NOT fix):** `selectedActions` uses lowercase `"issuetype"` (canonical field ID); `editedFieldsInput` uses camelCase `"issueType"` (bean name) — verbatim per Atlassian Bulk Ops FAQ, same as `labelsFields`/`"labels"`. Detail: `.factory/research/issue-331-issuetype-bulk-schema.md`.
  2. **Cross-project exit-64 guard (BC-3.4.019):** bulk `--type` requires all keys in ONE project (endpoint takes a single `issueTypeId`); guard fires in `handle_edit_bulk_fields` only when `--type` is present.
  3. **Name→issueTypeId resolution (project-scoped, no cache):** `GET …/createmeta/{proj}/issuetypes`, case-insensitive; unknown name → exit 64 listing valid types. One HTTP call per bulk `--type`. `issues.rs::get_issue_types_for_project`.
- **`jr issue move <KEY> <done-status>` proactive resolution enforcement (BC-3.2.013, ADR-0015):**
  `handle_move` (single-key only; bulk excluded) calls `get_transitions_with_fields` (`?expand=transitions.fields`) instead of `get_transitions`. When the resolved transition targets a done-category status (`to.statusCategory.key == "done"`) AND either `transition.fields` contains a `"resolution"` key OR `transition.is_conditional == Some(true)`, `jr` intercepts BEFORE the POST:
  - **Non-interactive (`--no-input` or stdin not a TTY):** exit 64, stderr: `The transition to "<to_label>" requires a resolution.` followed by a `Try:` hint line and `Run \`jr issue resolutions\`` suggestion (exact wording from `workflow.rs::handle_move` REQUIRED branch — load-bearing substrings: `"requires a resolution"`, `"--resolution"`).
  - **Interactive (TTY):** `dialoguer::Select` prompt listing resolutions from `load_resolutions(client, false)`.
  - **`--resolution <name>` supplied:** resolves via `resolve_resolution_by_name` (exact > prefix > substring), sends atomically in transition body. Ambiguous → exit 64 with candidate list.
  - **`--no-resolution` supplied:** explicit opt-out — proactive enforcement skipped, transition sent without resolution field (identical to pre-ADR-0015 default). Mutually exclusive with `--resolution` (clap → exit 2). Accepted silently on non-done-category transitions (no-op).
  - Bulk `jr issue move` is EXCLUDED — retains BC-3.2.009 reactive backstop only. Scripts doing bulk done-category moves should supply `--resolution` upfront.
  - `jr issue transitions --output json` output is byte-for-byte identical: `Transition.fields` and `Transition.is_conditional` carry `#[serde(skip_serializing)]` so they are never emitted in JSON serialization.
  - The reactive BC-3.2.009 backstop (400 "resolution is required") is preserved alongside BC-3.2.013 as a fallback.
  Detail: BC-3.2.013, ADR-0015, `docs/specs/issue-move-resolution.md`.
- **Markdown footnotes → ADF (`adf.rs`, issue #472):** `markdown_to_adf` enables `Options::ENABLE_FOOTNOTES`. ADF has no native footnote node, so the mapping is **preserve, not drop**: a reference `[^1]` becomes a *plain, unmarked* `[label]` text marker (via `push_footnote_marker` — deliberately does NOT inherit active marks, so a ref inside `**bold**` is not bolded); definitions are collected and flushed at `finish()` into an appended section (one `rule` divider + one `[label] `-prefixed paragraph each). Load-bearing details: (1) pulldown-cmark only emits `FootnoteReference` for *defined* labels — an undefined `[^x]` stays literal text upstream; (2) duplicate `[^1]:` lines are deduped by label (first wins) via `footnote_labels_seen`; (3) definitions render in definition-source order, not reference order (deliberate, deterministic); (4) the `[n]` marker can collide visually with literal `[n]` user text (accepted ADF-mapping limitation). **Empty-container pruning:** pulldown hoists footnote defs out of enclosing blocks, leaving empty shells (`> [^1]: x` → empty `blockquote`); `is_empty_block_container` (a free fn) prunes any `blockquote`/`heading`/`panel`/`listItem`/`bulletList`/`orderedList`/`table`/`tableRow` left with empty `content` (invalid ADF → Jira 400). `paragraph`/`codeBlock`/`tableCell`/`tableHeader` are excluded (empty is valid ADF / a placeholder paragraph keeps cells non-empty / pruning a cell would break column alignment). This also prunes a bare-`#` empty heading. Detail: `src/adf.rs` rustdoc + `adf::tests::test_markdown_footnote_*`.
- **Markdown minor constructs → ADF (`adf.rs`, issue #474):** `markdown_to_adf` also enables `ENABLE_SUPERSCRIPT | ENABLE_SUBSCRIPT | ENABLE_HEADING_ATTRIBUTES`. (1) **subsup:** `^x^` → `subsup` sup, `~x~` → `subsup` sub (rendered back by `adf_to_text` for a lossless round-trip). Enabling `ENABLE_SUBSCRIPT` reassigns *single*-tilde `~x~` from strikethrough to subscript; *double*-tilde `~~x~~` stays `strike` (pinned by `test_markdown_double_tilde_still_strikethrough_not_subscript`). Nested same-type spans (`^a ~b~ c^`) are deduped by `dedup_marks_by_type` so a text node never carries two `subsup` marks (ADF rejects duplicate mark types). **Limitations:** (a) pulldown does not open a superscript when `^` is tight against a preceding word char, so `mc^2^` stays literal — use `mc ^2^`; (b) `code` mark cannot coexist with `subsup`/`em`/`strong`/`strike` on one text node per the ADF schema (`code_inline_node`) — enforced at emission time since #571: `push_code` strips typographic marks from code spans (see BC-7.2.015); `` ^`x`^ `` and `` **`x`** `` now emit schema-valid ADF with the `code` mark only. (2) **Heading attrs:** `## Title {#id}` no longer leaks `{#id}` into text (ADF headings have no id; id/classes/attrs are parsed and dropped). GFM alerts (`> [!NOTE]`) → ADF `panel` shipped separately in #483 (below). Detail: `adf::tests::test_markdown_{superscript,subscript,heading_attributes}*` + `test_render_subsup_mark_reverse_path`.
- **Markdown GFM alerts → ADF `panel` (`adf.rs`, issue #483):** `markdown_to_adf` enables `ENABLE_GFM`, which in pulldown-cmark 0.13 gates *only* alert blockquotes (no side effect on the separately-set tables/strike/footnotes/subsup/heading-attr flags). A tagged alert arrives as `Tag::BlockQuote(Some(BlockQuoteKind))` → `panel`; plain `BlockQuote(None)` stays `blockquote`. **Kind → panelType** (`panel_type_for`, exhaustive — no `_` arm so a new pulldown variant is a compile error): Note→`info`, Tip→`success`, Important→`note`, Warning→`warning`, Caution→`error`. Only the five portable panelTypes are emitted; `tip`/`custom` are avoided (editor-flag-gated, inconsistent on Jira Cloud). **Content-model normalization (`normalize_panel_content`, mirrors #470's listItem pass):** ADF `panel.content` forbids nested `panel`, `table`, and `blockquote` — so nested panels/blockquotes are unwrapped recursively (inner panelType discarded) and tables are flattened to per-row paragraphs via `flatten_table_to_paragraphs`; `heading`/`paragraph` are kept but stripped of any node-level `marks` (`panel.content` requires no-marks). `normalize_list_item_content` gains a `panel` arm (ADF `listItem` forbids `panel` → unwrap). `panel` is in `is_empty_block_container`'s prune set (empty alert shell is invalid ADF). **Reverse path:** `adf_to_text` renders `panel` back to `> [!KIND]` via `gfm_label_for_panel_type` (inverse map); an unmapped panelType (`tip`/`custom`/foreign) falls back to a plain `> ` blockquote with no marker. Round-trip is stable for the five kinds (table/nesting normalizations are lossy, same class as listItem). **Parser leniency (verified empirically, looser than GitHub's spec):** the marker is recognized with or without the leading space (`>[!NOTE]`) and is case-insensitive (`[!note]`/`[!Note]`); the only disqualifiers are trailing text on the marker line (`> [!NOTE] extra`) and an unknown kind (`> [!FOO]`), both → plain blockquote. The renderer keys off `Some(kind)`, never string-matching the marker text. Spec: `docs/specs/adf-panel-content-model.md`; BC-7.2.009. Detail: `adf::tests::test_markdown_alert_*` + `test_render_panel_*` + `test_alert_markdown_to_text_roundtrip_all_kinds`.
- **Block-level HTML → literal text with `hardBreak` interior newlines (`adf.rs`, issues #489 + #492):** ADF has no raw-HTML node, so `markdown_to_adf` **preserves** block HTML rather than dropping it — symmetric with inline HTML. pulldown-cmark wraps a block run in `Tag::HtmlBlock` (Start/End) around per-line `Event::Html` events; `start()` routes `Tag::HtmlBlock` → `NodeKind::HtmlBlock` (NOT the `_ => Sink` catch-all, which silently discarded it before #489). On End the handler runs **Algorithm B** (BC-7.2.011): (1) concatenate accumulated Html-event text; (2) trim trailing `\r`/`\n` only (spaces/tabs preserved); (3) normalize `\r\n`→`\n`, lone `\r`→`\n`, then split on `\n`; (4) emit alternating `text` and `hardBreak` nodes (empty segments get `hardBreak` boundary, no text); (5) wrap in `paragraph`, trim leading/trailing `hardBreak`s; (6) if content array is empty after trim, return `EndResult::Empty` — no paragraph emitted (`paragraph` is excluded from `is_empty_block_container`, so this explicit guard is the operative prune path for EC-7). **Critical file-wide invariant (BC-7.2.011):** no `text` node may contain a raw `\n` character — Jira rejects them. Interior newlines are `hardBreak` nodes, never verbatim `\n` in text. Round-trip via `adf_to_text` is **line-structure-lossless but NOT byte-identical** in five cases: CRLF input (normalized to LF), leading newline (leading `hardBreak` trimmed), trailing newline(s) (stripped by step 2), trailing non-newline whitespace on final line (preserved forward, stripped by `AdfRenderer::finish().trim_end()` on reverse), and bare URL at an autolink boundary (rewritten to `[url](url)` by the #473 pass). Inline HTML (`Event::InlineHtml`) was already preserved via the shared `push_text` arm — this closes the block/inline asymmetry. Spec: `docs/specs/adf-block-html.md`; BC-7.2.011. Detail: `adf::tests::test_convert_block_html_is_preserved_as_literal_text` + `test_convert_multiline_block_html_preserves_interior_newlines` + `test_block_html_round_trips_through_adf_to_text` + `test_block_html_all_empty_block_emits_no_paragraph`.
- **`push_text`/`push_code`/`text_to_adf` CR/LF normalization chokepoint (`adf.rs`, issue #522, BC-7.2.011 INV-1):** `push_text` (Other context), `push_code`, and `text_to_adf` are the SELF-SUFFICIENT enforcement point for INV-1 — no raw `\r` or `\n` may appear in a non-codeBlock `text` node. In **Other context**, `\r\n`, lone `\r`, and bare `\n` are all mapped to a single space (mirrors `SoftBreak → " "`). In **codeBlock context**, `\r\n`→`\n` and lone `\r`→`\n` (newlines preserved as-is; codeBlock content is exempt from INV-1). HtmlBlock is exempt from both: it defers to Algorithm B (which produces `hardBreak` nodes instead). **Load-bearing asymmetry:** block HTML interior newlines → `hardBreak` nodes (Algorithm B); inline HTML (`Event::InlineHtml`, Other context) interior newlines → a **space** via `push_text`, consistent with `SoftBreak`. This closed a reachable HIGH bug (#522 CR-01): multi-line inline HTML in a user `--description` or comment (e.g. `foo <span\nx>bar`) previously emitted a raw `\n` into a text node → Jira HTTP 400. **Out of scope:** Unicode line separators U+2028/U+2029/U+0085/U+000B/U+000C are passed through verbatim — INV-1 covers ASCII `\r`/`\n` only.
- **ADF recursion-depth guard (`adf.rs`, SEC-001, CWE-674, PR #553, BC-7.2.012):** `MAX_ADF_DEPTH = 256` caps nesting depth on both the forward path (`markdown_to_adf` and its post-passes) and the reverse path (`adf_to_text`). Pathologically nested input (≥256 levels) exits 64 with a "nesting too deep" error instead of stack-overflowing (uncontrolled recursion → CWE-674). The guard fires in `normalize_list_item_content`, `normalize_blockquote_content`, `normalize_panel_content`, `assign_local_ids_walk`, `autolink_bare_urls`, and `AdfRenderer::render_node` — all recursive-descent sites. The inclusive depth-256 cap and the `>`→`>=` boundary correction were settled during dual code and security review (DEC-132): three guard sites initially used `depth > MAX_ADF_DEPTH` instead of `depth >= MAX_ADF_DEPTH`, so depth-256 inputs were accepted when the inclusive boundary should reject them — the guard fired only at depth 257 until corrected. `MAX_ADF_DEPTH` is `pub(crate)` — it is not accessible from integration tests; the constant regression pin lives in `src/adf.rs::test_max_adf_depth_constant_is_256`. Spec: BC-7.2.012. Detail: `tests/adf_recursion_depth.rs` (e.g. `test_issue_create_deep_markdown_description_exits_64`) + the depth-guard unit tests in `src/adf.rs` (e.g. `test_markdown_to_adf_depth_256_blockquote_is_err`, `test_adf_to_text_depth_256_is_err`).
- **GFM task lists → ADF `taskList`/`taskItem` (`adf.rs`, issue #471):** `markdown_to_adf` enables `Options::ENABLE_TASKLISTS`. The builder uses Approach B (post-hoc reclassification): lists start as `BulletList`; at `End(Tag::List)` the BulletList arm inspects children for `taskItem` nodes and reclassifies to `taskList`. **Critical event ordering (empirically confirmed, pulldown-cmark 0.13.3):** `Event::TaskListMarker(bool)` fires AFTER `Start(Tag::Item)` — in a TIGHT list it fires directly (item body is inline-only); in a LOOSE list it fires INSIDE a `Tag::Paragraph` wrapper. The retroactive stack mutation converts the current top (`Paragraph` or `ListItem`) to `TaskItem { checked }`. **Loose multi-paragraph task items (EC-16):** the `NodeKind::ListItem` arm detects when its first child is a `taskItem` JSON node (from loose-list paragraph promotion) and merges all subsequent paragraph children into one `taskItem` using `hardBreak` separators. **Nested sublists:** `TaskItem` finalization returns block children (`taskList`, `bulletList`, `orderedList`, and any other block node type) as `EndResult::WithHoists`; `BulletList` reclassification sees them as sibling entries in BulletList.children — nested `taskList` → `task_children` (EC-13), everything else → hoisted set (EC-15); `WithHoists` appends the `taskList` FIRST, then hoists, achieving `[taskList, bulletList]` doc-level ordering. No JSON side-channel fields. **`taskItem.attrs.state`:** `markdown_to_adf` ALWAYS emits uppercase `"TODO"` / `"DONE"` (Jira requires uppercase). `adf_to_text` is deliberately lenient: it uses `state.eq_ignore_ascii_case("DONE")` so externally-produced ADF with lowercase `"done"` still renders as `- [x]`. **localId assignment:** `assign_local_ids` DFS pre-order pass runs after `finish()`, before `autolink_bare_urls`; 1-based counter strings, document-wide unique, no uuid crate. **Pruning:** `"taskList"` and `"taskItem"` in `is_empty_block_container`; taskItem extended branch also prunes content containing only hardBreaks, whitespace text, and backslash-only text (the failed-escape artifact from `\\\n` in a tight list — a deliberate product choice). Spec: `docs/specs/adf-task-list.md`; BC-7.2.010. Detail: `adf::tests::test_markdown_task_*` + `test_task_*` + `test_nested_task_*`.
- **`allow_hyphen_values` on free-text CLI args (issue #471):** All user-authored free-text write-command inputs carry `allow_hyphen_values = true` (`src/cli/mod.rs`): `--summary` and `--description` (on `issue create` and `issue edit`), `worklog add --message`, the `issue comment add` and `issue comment edit` positional body text, and `issue remote-link --title`. This lets leading-dash free text (e.g. GFM task lists `- [ ] todo`, bullet lists `- item`, titled links `- important ref`) pass through clap without being treated as an unknown flag. **Tradeoff:** with `allow_hyphen_values`, a missing value (user forgets the string after `--description`) silently consumes the next token as the value instead of erroring. Prefer `--description="…"` (equals-sign form) or `--description-stdin` for programmatic/AI-agent usage where the value may start with a dash. `conflicts_with` enforcement (e.g. `--description` vs `--description-stdin`) is unaffected — the mutual-exclusion guard remains active.
- **Bare-URL autolinking → ADF `link` mark (`adf.rs`, issue #473):** pulldown-cmark 0.13 has **no autolink extension** (`ENABLE_GFM` adds only alert blockquotes, NOT GFM extended autolinks), so bare URLs arrive as plain text. `markdown_to_adf` runs a post-`finish()` pass — `autolink_bare_urls` → `split_text_node_on_urls` → `find_bare_url_spans`/`trim_url_extent` — that walks the built ADF tree and applies a `link` mark to bare-URL runs. **This is required, not cosmetic:** Jira's REST API does NOT auto-linkify plain-text URLs in a submitted ADF body (smart-link unfurl is a browser-editor-only, compose-time feature) — without the mark the URL is unclickable (Atlassian-confirmed; `.factory/research/issue-473-bare-url-autolink-scope.md`). **Scope is `http(s)://` explicit-scheme ONLY** (deliberate, #473): `www.`-prefixed hosts and bare emails are OUT of scope — they need scheme inference and carry high false-positive risk in prose (version strings `0.13`, file paths `src/adf.rs`, sentence-final domains), and because an applied mark permanently writes a link into the user's issue the narrowest covering scope wins. **Rules (GFM-derived):** (1) a URL starts only at text-node start or after whitespace/`*_~(` (so `foohttps://…` does NOT match); (2) extent runs to the next whitespace or `<`; (3) trailing `?!.,:*_~` are trimmed and a trailing `)` is dropped only when unbalanced (so `…/Foo_(bar)` keeps its parens but `(https://x)`/`https://x.` do not); (4) text nodes already carrying a `link` mark (`<url>` / `[t](url)`) or a `code` mark, and all `codeBlock` content, are skipped — never double-linked, never linkified inside code; (5) the scheme is matched **case-insensitively** (`HTTPS://`, `Http://`, `httpS://` all link — `find_bare_url_spans` searches a `to_ascii_lowercase` copy whose byte offsets map 1:1 to the original since only ASCII A–Z fold), and the `href` is sliced from the original text so the user's casing is preserved. Existing inline marks (strong/em/…) are preserved on the split URL node. **Deviations from GFM (deliberate, bias toward fewer false positives):** (a) the boundary "before" set is `*_~(`+whitespace+start ONLY — GFM also admits `[`, which we omit (so an unresolved `[https://x]` shortcut stays plain); (b) because the pass runs over the *already-built* tree, a URL whose interior contains inline markup (`https://x/a*b*c`, `*b*` parsed as emphasis) is already split into separate text nodes, so only the leading plain run links (`https://x/a`) — pinned by `test_bare_url_split_by_emphasis_links_only_leading_run`. Round-trip note: a bare URL now renders back through `adf_to_text` as `[url](url)` (it IS a link), not the bare string. Detail: `adf::tests::test_bare_*` + `test_url_in_*_not_linkified` + `test_www_url_stays_plain_text` + `test_bare_email_stays_plain_text`.
- **`sanitize_attachment_filename` — CWE-22 disk-path sanitization (BC-2.7.011, S-576-2, SEC-576-001/002):** `sanitize_attachment_filename(name: &str) -> Option<String>` in `src/cli/issue/attachments.rs` is the DISK-PATH sanitization function for attachment filenames. **Distinct from `display_sanitize_filename`** (CWE-116 display-safety, S-576-1) — do NOT merge or share logic. The 5-step algorithm: (1) basename extraction via `Path::file_name()`, then split on `\` for Windows paths (on Unix `\` is not a separator so `Path::file_name()` does not strip it); (2) pseudo-name rejection — `"."`, `".."`, or empty → `None`; (3) NUL byte rejection — contains `'\0'` → `None` (NOT stripped, REJECTED entirely); (4) character scrub — replace `/`, `\`, `:` with `_`; (5) length cap to 214 bytes on a valid UTF-8 char boundary, then strip trailing ASCII whitespace and `.`. **Containment check (SEC-576-002):** path containment check (final path within intended directory) is the CALLER's responsibility, not this function — `sanitize_attachment_filename` does not validate that the joined path stays within `base_dir`. **Windows device-name escape (SEC-576-001):** `sanitize_attachment_filename` does NOT escape `CON`, `NUL`, `COM1`, etc. — it returns `Some("CON")` / `Some("NUL")` / etc. unchanged. Device-name escape (`_` prefix → `_CON`, `_NUL`) is applied at the **single-id** bare-naming call site only (`handle_single_download`); batch paths use a SHA-1 prefix that inherently disambiguates. **Mutation exemption:** `prop_sanitize_attachment_filename_no_path_traversal` proptest (VP-576-001) covers this function's security invariants — it is in `.cargo/mutants.toml` examine_globs so cargo-mutants covers its mutations.
- **`jr issue attachment download` redirect behavior — GHSA-9857-6MW7-FQ2M and JRACLOUD-97046 (S-576-2):** Step 2 of the two-step download issues `GET /rest/api/3/attachment/content/{id}`. **MUST NOT** append `?redirect=false` (JRACLOUD-97046 — breaks some binary file formats; download endpoint returns the file directly or redirects to a CDN URL). reqwest follows 302/303 redirects automatically (correct behavior). On cross-host CDN redirect (e.g., Atlassian API → S3 or similar CDN), reqwest 0.13 strips `Authorization` and `Cookie` headers (GHSA-9857-6MW7-FQ2M) — this is **CORRECT** behavior for CDN downloads: the CDN authenticates the signed URL in the redirect target, not the original auth header; forwarding the header would be a credential-leak vulnerability. Do NOT attempt to re-attach `Authorization` on redirect, and do NOT use a custom redirect policy to suppress this behavior. The test `test_bc_2_7_007_auth_absent_on_redirect_target` asserts `Authorization` is absent on CDN-side requests; this assertion documents correct behavior, not a bug. **Step 2 always uses the platform URL** (`/rest/api/3/attachment/content/{id}`) — never uses the `content` URL field from the metadata response (JSDCLOUD-10841: for JSM service desk attachments the `content` field may return a `servicedeskapi` URL that behaves differently).
- **`AttachmentMetadata.id` accepts integer or string (FIX-576-DL, mock-vs-live drift, run 30031724733):** `GET /rest/api/3/attachment/{id}` (step-1 metadata) returns `"id"` as a JSON **integer** on live Jira Cloud (e.g. `10008`), while `GET /rest/api/3/issue/{key}?fields=attachment` (list endpoint) returns it as a **string**. All S-576-2 wiremock tests used string IDs, so the drift was invisible until the first live validation run, which produced `invalid type: integer \`10008\`, expected a string at line 1 column 11`. The `deserialize_string_or_int_as_string` visitor on `AttachmentMetadata.id` in `src/api/jira/attachments.rs` normalises both to `String` without touching `AttachmentObject.id` (string-only, list path). Pinned by `tests/attachment_download.rs::test_download_integer_id_in_metadata_succeeds` (integer wire) and `test_download_string_id_in_metadata_still_succeeds` (string regression guard).
- **`jr issue attachment upload` multipart retry constraint (ADR-0017, S-576-3):** `POST /rest/api/3/issue/{key}/attachments` requires `X-Atlassian-Token: no-check` on every request. `Request::try_clone()` returns `None` for multipart bodies — on a 429 retry the entire request must be rebuilt from scratch: reopen `tokio::fs::File::open(path)` for every file, construct a new `reqwest::multipart::Form` with fresh `ReaderStream` parts, and POST again. The retry loop in `src/api/jira/attachments.rs::upload_attachments` enforces this. Do NOT attempt to cache the body or share a `Form` across retries.
- **`jr issue attachment upload` `X-Atlassian-Token: no-check` is mandatory (SEC-576-003, S-576-3):** Jira rejects multipart uploads without `X-Atlassian-Token: no-check` (its XSRF protection applies to non-browser clients too). The header must be on every upload POST. Tested by `test_bc_3_9_001_multipart_post_x_atlassian_token_mandatory` — do not remove or conditionalize it.
- **`jr issue attachment upload` SEC-576-004 CRLF/NUL/double-quote/backslash filename guard (S-576-3, CWE-93, F5-R1-006, F5-R2-002):** `\r`, `\n`, `\0`, `"`, and `\` in the server-supplied filename are mapped to `_` before passing to `Part::file_name()` in `Content-Disposition`. CR/LF/NUL prevent header-line injection; `"` prevents quoted-string parameter boundary breakage (a raw `"` prematurely terminates the `filename=` value, allowing parsers to misread subsequent data as a new header parameter); `\` is the RFC 2616 quoted-string escape character — a stray `\` lets a parser misread the next character as an escaped sequence. The guard is in `src/api/jira/attachments.rs::upload_attachments` and `src/api/jsm/attachments.rs::attach_temporary_file`. Distinct from `display_sanitize_filename` (CWE-116) and `sanitize_attachment_filename` (CWE-22 disk path) — each guard is at its own layer. Tested by `test_sec_576_004_content_disposition_crlf_injection_guard`, `test_f5_r1_006_safe_name_double_quote_mapped_to_underscore`, and `test_f5_r2_002_safe_name_backslash_mapped_to_underscore`.
- **JRACLOUD-96384 — multiple same-filename attachments can coexist in Jira (S-576-3, BC-3.9.010):** Uploading a file whose name already exists does NOT replace the old one — both coexist. `--replace-existing` deletes ALL attachments matching the filename (case-sensitive, exact match) BEFORE the upload (VP-576-003 ordering invariant: all DELETEs complete before the POST). A dry-run with `--replace-existing` reflects this in `wouldDelete`. Do NOT cite JRACLOUD-96384 in user-visible strings without Perplexity-validation.
- **`jr issue attachment delete` targeted-vs-bulk 404 asymmetry (DEC-168, S-576-4; BC-3.9.008 vs BC-3.9.010):**
  Single-AID targeted delete uses `delete_attachment_targeted` (`src/api/jira/attachments.rs`). On 404 this function returns `JrError::UserError("Attachment <AID> not found or not accessible.\n<Jira error body>")` (exit 64) — the canonical prefix is followed by the raw Jira error body so callers can surface it. This is **intentionally different** from `delete_attachment` (S-576-3 replace path): that function maps 404 → `JrError::UserError("…not found or already deleted.")` — benign-skip semantics for the replace-existing loop. Do NOT unify these two functions or change `delete_attachment`'s benign-skip behavior (the S-576-3 replace-detection check `msg.contains("not found or already deleted")` would break). Bulk delete paths in S-576-4 (`handle_attachment_delete`) use the existing `delete_attachment` directly (so bulk 404 stays benign — BC-3.9.010).
- **`get_attachment_metadata` 404 body-surfacing asymmetry (BC-2.7.012, F5-R3-001):** `get_attachment_metadata` (`src/api/jira/attachments.rs`) returns `JrError::ApiError { status: 404, message }` for 404 (body preserved, NOT wrapped in UserError). **Call sites** format the error differently per BC-2.7.012: (a) `handle_single_download` (download path) maps 404→`UserError("Attachment <id> not found or not accessible.")` — canonical-only, no body; (b) `handle_attachment_delete` interactive confirmation gate maps 404→`UserError("Attachment <id> not found or not accessible.\n{body}")` — canonical prefix + Jira error body (DEC-168 body-surfacing contract); (c) bulk dry-run `Err(_)` fallback swallows all errors and uses an id-only row (body irrelevant). Do NOT consolidate these three call sites or re-add body appending inside `get_attachment_metadata` itself — the download path MUST NOT show the body (BC-2.7.012 §"download path canonical-only").
- **`allow_hyphen_values` MUST NOT be set on `AttachmentSubcommand::Upload`'s `file` positional (S-576-3):** The `file` field is `Vec<PathBuf>` with `num_args = 1..` (greedy variadic). Setting `allow_hyphen_values = true` causes clap to consume subsequent flags like `--output` as file path values, producing spurious "file not found: --output" exit-64 errors. Bare `-` (stdin sentinel) works without `allow_hyphen_values` because clap treats a lone `-` as a value, not a flag prefix. This is an intentional deviation from the `allow_hyphen_values` convention applied to free-text string args (`--summary`, `--description`, etc.).
- **`jr issue attachment upload --public/--internal` JSM two-step flow (S-576-5, BC-3.9.003):** Setting `--public` or `--internal` routes through `handle_attachment_upload_jsm` (NOT the platform `upload_attachments` path). **Step 0 (P1-004):** `GET /rest/api/3/issue/{key}?fields=project` → `fields.project.key` (HTTP, validates issue exists). 404 → exit 64 "Issue {key} not found or not accessible." Implemented by `src/api/jira/issues.rs::JiraClient::get_issue_project_key`. Do NOT revert to the old `extract_project_key` string-split — it bypassed issue existence validation. Step 1: POST `…/servicedesk/{sdId}/attachTemporaryFile` (multipart, one call per file, `X-Atlassian-Token: no-check` mandatory). Step 2: POST `…/request/{key}/attachment` with `{"temporaryAttachmentIds":[…],"public":<bool>}`. The `public` bool comes from the `--public` flag; `--internal` sets `public=false`. Service desk ID resolution uses `get_or_fetch_project_meta` (`src/api/jsm/servicedesks.rs`) which matches `ServiceDesk.project_id: String` equality — NOT projectKey. `JsmUploadOpts` struct bundles the five boolean flags to stay under the 7-arg clippy limit.
- **`--public` on non-JSM exits 64; `--internal` on non-JSM is silent no-op (OQ-9, EC-3.9.003-7, S-576-5):** The project type check fires AFTER `get_or_fetch_project_meta` but BEFORE the visibility gate and BEFORE any dry-run preview. For `--internal` on non-JSM: falls through to the exact same platform upload path as if neither flag was set. The guard is in `handle_attachment_upload_jsm::meta.project_type != "service_desk"` branch. Issue key lookup (P1-004) fires FIRST — before the project meta check.
- **SEC-576-006 stale-ID self-heal (`src/api/jsm/attachments.rs::attach_temporary_file`, S-576-5):** On 404 or 403 from step-1, `handle_attachment_upload_jsm` calls `cache::invalidate_project_meta_cache`, re-fetches project meta, updates `current_sd_id`, and retries ONCE. Second failure (P1-001): 404 → `JrError::UserError("Service desk for {project_key} not found after refresh.")` (exit 64); 401 → `JrError::NotAuthenticated` (exit 2); others propagate as-is. Stale heal fires at most once per upload command invocation (`stale_healed: bool` guard). Do NOT remove the `stale_healed` guard — it prevents an infinite retry loop on persistent 404/403.
- **BC-3.9.006 step-2 failure taxonomy (`src/api/jsm/attachments.rs::post_request_attachment`, S-576-5):** 401 → `JrError::NotAuthenticated` (exit 2) + retry hint; 403 → `JrError::ApiError { status: 403 }` (exit 1) + hint; other 4xx → `JrError::UserError` (exit 64) + hint; 5xx → `JrError::ApiError` (exit 1) + retry hint; transport/network error → `JrError::NetworkError` (`"Could not reach {host} — check your connection"`, exit 1, NO expired-ID hint — parity with step-1 `attach_temporary_file` transport mapping; F5-R1-007/FIX-F5-006). Retry hint: `"Temporary attachment IDs may have expired. Try the upload again."` — scoped to the HTTP branches (401, 403, 4xx, 5xx); the `NetworkError` branch does not carry it. HTTP branches are pinned by sub-assertions 6–10 of `test_bc_3_9_006_jsm_upload_error_taxonomy` in `tests/attachment_jsm.rs` (which uses `mount_as_scoped` because wiremock 0.6 uses FIFO ordering — first-registered mock wins for equal-priority mocks on the same path); transport mapping pinned by `src/api/jsm/attachments.rs::tests::test_f5_r1_007_step2_network_error_uses_canonical_network_error_variant`.
- **VP-576-005 combined gate (S-576-5):** When `--public + --replace-existing`, ONE combined prompt fires (not two). The attachment list GET runs before the gate (interactive and non-interactive) to determine which consumer fires: consumer-3 (combined prompt) when ≥1 filename match exists, consumer-1 (`--public`-only prompt) when zero matches. Non-interactive without `--yes`: exits 64 with the consumer-appropriate hint. `--yes` bypasses the gate display but the list GET still runs when `replace_existing=true` (needed to build `to_delete` for the VP-576-003 DELETE loop). For `--internal + --replace-existing`: consumer-2 (replace-only prompt) fires when ≥1 match; no gate when zero matches.
- **`jr issue comment edit --internal/--public` uses MERGE semantics on Jira's properties endpoint (BC-3.5.006, JSDCLOUD-6050):** A `PUT /rest/api/3/issue/{key}/comment/{id}` that includes a `properties` array MERGES with existing properties — it does NOT replace them. A body-only PUT (no `--internal`/`--public` flag) sends no `"properties"` key at all, which leaves existing visibility UNCHANGED (PRESERVED semantics). Confirmed by `.factory/research/issue-577-properties-merge-replace-2026-07-09.md`. Consequence: `--public` will set `sd.public.comment={internal:false}` but will NOT clobber an unrelated `jr.test.marker` property. Do NOT attempt to clear visibility by omitting properties; use `--public` explicitly. `update_comment` (`src/api/jira/issues.rs::update_comment`) enforces this: `None` → sends only `{"body":<adf>}` (no `"properties"` key); `Some(true)` → MERGE-sets `internal=true`; `Some(false)` → MERGE-sets `internal=false`. Tested by `test_e2e_comment_edit_visibility_merge_semantics` in `tests/e2e_live.rs` (gated; requires `JR_E2E_JSM_PROJECT`).

## AI Agent Notes

- `JR_BASE_URL` env var overrides the configured Jira instance URL (used by tests to inject wiremock). **Debug builds only** — release binaries ignore this env var. The override is gated via `#[cfg(debug_assertions)]` at BOTH read sites — `Config::base_url()` in `src/config.rs` and `JiraClient::from_config()` in `src/api/client.rs` — because either site alone leaves a token-leak vector where `JR_BASE_URL=http://attacker/` would redirect authenticated requests to a non-Atlassian host. Regression-pinned by `tests/base_url_release_gate.rs`. Mirrors the existing `JR_AUTH_HEADER` gate (SD-002).
- `JR_TENANT_INFO_URL` env var overrides the actual network target of `src/api/jira/tenant.rs::fetch_cloud_id`'s `GET {site}/_edge/tenant_info` request (used by tests to inject wiremock, since wiremock has no HTTPS/TLS support and `fetch_cloud_id` requires `site_url` to start with `https://`) — `site_url` itself still drives the `https://`-prefix precondition; only the actual request target is redirected. **Debug builds only** — release binaries ignore this env var. Gated via `#[cfg(debug_assertions)]` at its single read site in `fetch_cloud_id` — SECURITY-CRITICAL like `JR_BASE_URL`, not a convenience seam: an ungated read would let an attacker who controls the process environment redirect the unauthenticated tenant_info lookup to a host of their choosing and have the returned `cloudId` persisted into the profile, reopening the wrong-tenant-misdirection vector the `https://`-only precondition and `redirect::Policy::none()` (ADR-0022 §1, Pass-4/Pass-1 adversarial review Findings #4/#12) otherwise close for the real network path. Regression-pinned by `tests/jr_tenant_info_url_release_gate.rs`. (S-cycle4-cloud-id-correctness, ADR-0022, BC-1.2.052)
- `JR_BULK_UNKNOWN_GRACE_SECS` overrides the unknown-bulk-task grace period (default 30s). Debug-only, single-site gate in `bulk.rs::resolve_unknown_status_grace` (not security-critical). Pinned by `tests/bulk_unknown_grace_release_gate.rs`. (#336)
- `JR_BULK_AWAIT_TIMEOUT_SECS` overrides the bulk-poll wall-clock timeout (default 300s). Debug-only, single-site gate in `bulk.rs::resolve_bulk_await_timeout`. Pinned by `tests/bulk_await_timeout_release_gate.rs`. (#333)
- `JR_E2E_ENABLED` — GitHub Actions **repository variable** (`vars.JR_E2E_ENABLED`). Gates the `e2e:` job at scheduling time. NOT a Rust env var; never read by `src/` code. Forks with this variable unset skip cleanly (empty string `!= 'true'`). The canonical repo sets `JR_E2E_ENABLED=true` as a repository variable in GitHub repo settings (not environment-scoped — environment-level variables are not available in `jobs.<id>.if:`). See `docs/specs/e2e-fork-safe-ci-enablement.md §2.3`.
- **`GITLEAKS_DISABLED`** — GitHub Actions **repository variable**
  (`vars.GITLEAKS_DISABLED`). Skips the gitleaks secret-scan job on
  pull-request events when set to `'true'` (`ci.yml` `jobs.security.if:
  github.event_name == 'pull_request' && vars.GITLEAKS_DISABLED != 'true'`).
  Provided for forks that cannot obtain a gitleaks org/commercial license or
  prefer an alternative secret scanner. NOT a Rust env var; never read by `src/`
  code. Follows the `vars.JR_E2E_ENABLED` repo-variable-gate doc pattern
  (`docs/specs/e2e-fork-safe-ci-enablement.md`). See
  `docs/specs/fork-friendly-release-ops.md`.
- `JR_CONFIG_DIR` env var overrides the config directory in debug builds (cross-platform test isolation seam; see BC-6.2.017). Debug builds only — release binaries ignore this env var. The override is read before the `#[cfg(windows)]` / `#[cfg(not(windows))]` OS split in `src/config.rs::global_config_dir()`, so it works identically on all platforms. Pinned by `tests/config_dir_release_gate.rs`.
- `JR_CACHE_DIR` env var overrides the cache root directory in debug builds (cross-platform test isolation seam; see BC-6.2.017). Debug builds only — release binaries ignore this env var. Identical gate pattern to `JR_CONFIG_DIR`, applied in `src/cache.rs::cache_root()`. Pinned by `tests/config_dir_release_gate.rs`.
- `JR_SERVICE_NAME` env var overrides the keychain service name in debug builds (test isolation seam — lets keyring integration tests scope their own namespace without touching a developer's real `jr-jira-cli` entries). Debug builds only — release binaries ignore this env var and always use the compiled-in default `"jr-jira-cli"`. Gated via `#[cfg(debug_assertions)]` at `src/api/auth.rs::service_name()` (SEC-JR-SERVICE-NAME-GATE). Pinned by `tests/jr_service_name_release_gate.rs`. In `tests/oauth_refresh_integration.rs` (keyring-gated, `#[ignore]`) `JR_SERVICE_NAME` is set in-process via `set_env` serialized by the `env_lock` mutex, rather than a subprocess `.env()` call — safe because those tests are gated and sequential.
- `JR_STDIN_IS_TTY` env var forces TTY mode in debug builds (interactive-branch test seam;
  when set to `"1"`, suppresses the `src/main.rs` auto-`--no-input` flip that fires on
  non-TTY stdin; release builds ignore this env var). Gated via `#[cfg(debug_assertions)]`
  at `src/main.rs` (seam scope). Pinned by `tests/jr_stdin_is_tty_release_gate.rs`.
  Used by interactive confirmation tests in `tests/comment_delete.rs` (VP-577-013,
  VP-577-030) and `tests/comment_edit.rs` (VP-577-029 — S-577-5 interactive `--public`
  confirmation gate).
- `JR_TEST_BLOCK_UNTIL_SIGINT` env var is the readiness-handshake seam for the SIGINT
  subprocess test (`tests/interrupt_signal.rs`, VP-MUTANTS-SCOPE-1-001). Debug builds
  AND Unix only — gated `#[cfg(all(debug_assertions, unix))]` at BOTH the env read and
  the seam fn (`src/main.rs::block_until_sigint_test_seam`); release and non-Unix builds
  compile the code path out entirely and never read the env var. When set to `1`, `run()`
  selects a `signal(SIGINT)`-based shutdown future instead of `tokio::signal::ctrl_c()`,
  prints a `JR-TEST-READY` marker (flushed) once the listener is registered, then blocks
  so the test can send SIGINT deterministically — no fixed `sleep`. Like every sibling
  seam above (`JR_STDIN_IS_TTY`, `JR_BASE_URL`, `JR_CONFIG_DIR`, `JR_CACHE_DIR`,
  `JR_SERVICE_NAME`), this seam carries a `tests/*_release_gate.rs` pin —
  `tests/jr_test_block_until_sigint_release_gate.rs` — asserting `#[cfg(debug_assertions)]`
  (present here as part of the seam's stricter `#[cfg(all(debug_assertions, unix))]` gate)
  appears within 5 source lines of the `JR_TEST_BLOCK_UNTIL_SIGINT` env-var read in
  `src/main.rs`, closing adversarial pass-8 finding LOW-2 and making this seam fully
  convention-consistent with its siblings. This seam remains non-security (contrast
  `JR_BASE_URL`, whose release-gate pin is SECURITY-CRITICAL — redirecting authenticated
  requests to an attacker-controlled host): it only makes `run()` block awaiting SIGINT and
  print a readiness marker, and does not redirect I/O, auth, or any config/cache path. It
  is additionally narrowed by the `unix` conjunct on top of `debug_assertions`, unlike its
  siblings' bare `debug_assertions` gate.
- `JR_FORCE_DPAPI_FALLBACK` env var (debug builds only; S-cycle4-dpapi-storage-fix, ADR-0021
  §1) lets a Linux/macOS debug build exercise `store_oauth_tokens`'s `TooLong`-routing arms
  for testing — `#[cfg(not(windows))] fn engage_dpapi_fallback` (`src/api/auth.rs`) returns
  `auth_windows_store::should_fallback_to_dpapi(err)` instead of the hardcoded `false` every
  release build (and every debug build with the env var unset) uses. Release binaries ignore
  this env var entirely (compiled out via `#[cfg(debug_assertions)]`); production non-Windows
  behavior is unaffected by the seam's mere existence. Pinned by
  `tests/jr_force_dpapi_fallback_release_gate.rs`. Security-relevant in the same sense as
  `JR_BASE_URL`: it gates a credential-storage routing decision, though only in debug builds.
- `JR_S759_FORCE_TOOLONG` env var (debug builds only; S-cycle4-dpapi-storage-fix), values
  `"access"`/`"refresh"` — models `JR_S303_PERSIST_FAIL`'s shape. When set to one of those
  values, the corresponding `set_password` call inside `store_oauth_tokens` is skipped and a
  synthetic `keyring::Error::TooLong` is returned instead, letting a non-Windows debug build
  simulate the one `keyring::Error` variant no real macOS/Linux keyring backend can actually
  produce — needed to exercise AC-001-006/AC-019's delete-keyring-first / rollback / DPAPI
  routing behavior in keyring-gated tests off Windows. Gated via `#[cfg(debug_assertions)]` at
  `src/api/auth.rs::forced_toolong()`. Pinned by
  `tests/jr_s759_force_toolong_release_gate.rs`.
- `JR_FORCE_DPAPI_LOAD_PAIR` env var (debug builds only; S-cycle4-dpapi-storage-fix), values
  `"found"`/`"corrupt"`/`"backend_error"` — lets a non-Windows debug build exercise
  `load_oauth_tokens`'s DPAPI-fallback read-path branches (AC-009/AC-010/AC-011, BC-1.4.036),
  none of which are otherwise reachable off Windows in production
  (`auth_windows_store::load_pair`'s `#[cfg(not(windows))]` arm is hardcoded `Ok(None)` once
  the profile-name path-traversal guard passes). The guard call still runs FIRST, before the
  seam is consulted, so a guard-rejecting profile name is rejected regardless of the seam's
  value. Gated via `#[cfg(debug_assertions)]` at
  `src/api/auth_windows_store.rs::load_pair()`'s `#[cfg(not(windows))]` arm. Pinned by
  `tests/jr_force_dpapi_load_pair_release_gate.rs`.
- **Release-ops repo-variable gates** — `SIGNING_ENABLED`, `HOMEBREW_TAP_REPO`, `RELEASE_GAP_FILL_ENABLED`, `SYNC_UPSTREAM_REPO` (all GitHub Actions repository variables, never read by `src/` code) gate the opt-in signing/backfill/gap-fill/fork-sync workflows. All unset in the canonical repo → those workflows are no-ops; downstream forks opt in. Same fail-safe pattern as `JR_E2E_ENABLED`. See `docs/specs/fork-friendly-release-ops.md`.
- **When adding a new `JR_*` test-seam env var:** grep `CLAUDE.md` for existing `JR_*` entries and add a parallel line in the SAME commit as the code change. This is the codified doc-fallout pattern from #335/#357; first applied retroactively when `JR_BULK_UNKNOWN_GRACE_SECS` and `JR_BULK_AWAIT_TIMEOUT_SECS` shipped without documentation.
- **Citation discipline for external-tracker IDs in user-facing strings:** before citing a JRACLOUD-*/GitHub/community ID in anything a user sees (stderr, errors, JSON, hints) or in literal rustdoc, Perplexity-validate the source actually documents the symptom — issue #361 had three misattributed JRACLOUD tickets survive multiple PRs. Also ensure the string is valid in the user's env (e.g. JQL allows one ORDER BY) and keep paraphrasing rustdoc in lockstep.
- **Citation form in spec/CLAUDE.md:** prefer symbol-form (`<file>::<fn>` or `… § "<comment>"`) over line numbers, which drift on refactor. Fall back to `<file>:~NN` (`~` = approximate); never a bare `<file>:NN-MM` for new citations. (#408)
- `JR_PROFILE` env var overrides the active profile per-call (combine with direnv to scope a repo to a sandbox site)
- `--profile NAME` flag overrides `JR_PROFILE` for one invocation; precedence is flag > env > config > "default"
- `JiraClient::new_for_test(base_url, auth_header)` constructs a client for integration tests
- Test fixtures live in `tests/common/fixtures.rs`
- Keyring round-trip tests are gated behind `JR_RUN_KEYRING_TESTS=1` + `#[ignore]` (Linux CI may lack secret-service; macOS prompts on novel service names). Coverage in `src/api/auth.rs` (inline), `tests/auth_profiles.rs` (`auth_login_creates_new_profile_with_url`, `auth_login_with_jr_profile_pointing_to_unrelated_profile_still_creates_target`, `global_profile_flag_targets_auth_status`), `tests/multi_cloudid_disambiguation.rs`, `tests/oauth_refresh_integration.rs`. Run: `JR_RUN_KEYRING_TESTS=1 cargo test -- --include-ignored`.
- OAuth integration tests in `tests/oauth_embedded_login.rs` are gated behind `JR_RUN_OAUTH_INTEGRATION=1` + `#[ignore]`. The test is currently `unimplemented!()` — it requires a wiremock base-URL override in `oauth_login` before a real assertion can be written. CI does not run it; the embedded-creds smoke test in `release.yml` covers the binary-level check instead.
- **Two-layer E2E gate:** `JR_E2E_ENABLED` (workflow level, repo variable) determines whether the CI job starts; `JR_RUN_E2E` (test-binary level, process env) determines whether individual `#[ignore]` tests run. The workflow sets `JR_RUN_E2E=1` only when the job starts. Each layer independently provides fail-safe behavior. See `docs/specs/e2e-fork-safe-ci-enablement.md §2.1`.
- Live-Jira E2E tests (`tests/e2e_live.rs`) are gated behind `JR_RUN_E2E=1` + `#[ignore]` + a per-test `if !e2e_enabled() { return; }` early-return (S-410). Inert in `cargo test`/`ci.yml` (never `--include-ignored`); gate correctness pinned by `test_e2e_gate_disabled_when_env_unset` + `test_every_ignored_test_has_gate_guard`. Runs only in `.github/workflows/e2e.yml` (non-blocking). Local: `JR_RUN_E2E=1 JR_E2E_BASE_URL=… JR_AUTH_HEADER=… JR_E2E_PROJECT=E2E cargo test --test e2e_live -- --include-ignored --test-threads=1`. **Full env-var table: `docs/specs/e2e-live-jira-testing.md`.** Quick reference (all debug-only seams except `JR_RUN_E2E`):
  - Required: `JR_RUN_E2E`, `JR_E2E_BASE_URL` (→ `JR_BASE_URL`), `JR_AUTH_HEADER` (composed from `JR_E2E_EMAIL`+`JR_E2E_API_TOKEN`), `JR_E2E_PROJECT`.
  - Optional: `JR_E2E_BOARD_ID` (sprint tests), `JR_E2E_JSM_PROJECT` (JSM tests — value is `EJ`; the JSM-gated test functions are listed in `docs/specs/e2e-live-jira-testing.md §4`; set as environment variable in `jira-e2e` GitHub Environment, NOT a repository variable), `JR_E2E_JSM_RESOLUTION` (resolution name override for `jsm_self_close` teardown and `test_e2e_jsm_resolution_enforcement`; auto-discovered from `jr issue resolutions` when unset — S-JSM-E2E-3), `JR_E2E_STATUS_DONE`/`_IN_PROGRESS` (default Done/In Progress), `JR_E2E_ISSUE_TYPE` (default Task), `JR_E2E_ISSUE_TYPE_ALT` (bulk issueType round-trip; clean-skips if unset), `JR_E2E_POLL_MAX_ATTEMPTS`/`_INITIAL_MS` (test-side poll loop; defaults 5/250ms), `JR_E2E_PARENT_KEY` + `JR_E2E_CHILD_TYPE` (paired; enable `create --parent` test — E2E-HV-2; clean-skip if either unset), `JR_E2E_EDIT_FIELD` (`NAME=VALUE`; enables `edit --field` test — E2E-HV-2; clean-skip if unset). The story-points round-trip test (`create/edit --points`, `--no-points`) needs no env var — it auto-discovers the Story Points field id via `jr api /rest/api/3/field` and writes a temp config, clean-skipping when no such field exists or it is absent from the Create screen.
- **JSM write-test teardown convention (S-JSM-E2E-2 + S-JSM-E2E-3):** Tests that create JSM requests (Scenarios 5+6+8 and the JSM attachment tests — see `docs/specs/e2e-live-jira-testing.md §4`) self-close via `jsm_self_close(key, &h)` in the test body — NOT via the label-based CI sweeper and NOT via the hardcoded `status_done()` name. Root cause for the helper: the EJ JSM workflow has no transition named "Done" (live run 26839267723 confirmed: tests passed green but left ~2 open EJ tickets). `jsm_self_close` dynamically discovers a closing transition via `jr issue transitions <key> --output json`, selects the first transition with `to.statusCategory.key == "done"` (the stable Jira-wide machine constant for a closing/green status — covers Resolved, Closed, Canceled regardless of workflow name), then attempts resolution discovery (S-JSM-E2E-3): it runs `jr issue resolutions --output json` and passes `--resolution <R>` on the move to produce properly-resolved tickets. Resolution name source: `JR_E2E_JSM_RESOLUTION` env override first, else first `name` from the resolutions list. If resolution discovery fails for any reason, falls back to move WITHOUT `--resolution` (S-JSM-E2E-2 behavior). The `status_done()` helper and its use in ES-project tests remain unchanged — ES Scrum correctly has a transition named "Done". Labels do NOT reliably propagate from `JsmRequestBuilder::build()` to the Jira issue `labels` field, so the sweeper cannot catch EJ-created requests. Self-close is best-effort: all failure paths emit `eprintln!("[WARN] jsm_self_close: …")` and return `()`; the test never fails on close failure. Residual orphan risk LOW and accepted (EJ issues are inert; Resolution-screen transition may still fail; see `docs/specs/jsm-e2e-coverage.md §6.3`). Do NOT extend the sweeper for EJ without first fixing label propagation.
- **JSM resolution proactive enforcement (BC-3.2.013, ADR-0015):** `jr issue move` intercepts done-category transitions BEFORE the POST: in non-interactive mode (`--no-input` or stdin not a TTY), exits 64 with `The transition to "<to_label>" requires a resolution.` + `--resolution` hint. Interactive: `dialoguer::Select` prompt. Supply `--resolution <name>` to send the resolution atomically in the transition body (BC-3.2.011); supply `--no-resolution` to opt out of enforcement. The reactive BC-3.2.009 backstop (API 400 "resolution required") is preserved alongside. Run `jr issue resolutions` to discover available resolution names. See `docs/specs/jsm-e2e-coverage.md §5 Scenario 8` for the E2E enforcement test.
- **`Drop`-guard best-effort teardown pattern (`AttachmentDropGuard`, S-576-6; `ComponentDropGuard`, S-COMP-E2E-1):** live E2E tests that create a fixture needing explicit cleanup (an uploaded attachment, a throwaway `jr component create` fixture) use a small `Drop`-impl struct rather than a bare teardown call at the end of the test function — the struct's field(s) default to `None` (no cleanup performed), are populated IMMEDIATELY after the corresponding creation call succeeds (never before, and never skipped even on an early return — an unpopulated guard performs no cleanup by design), and `drop()` spawns a FRESH `E2eHarness::new()` (never borrows the outer test's own harness, since that borrow would not survive a panic-unwind) to issue the best-effort cleanup call. Every failure path inside `drop()` emits `eprintln!("[WARN] <GuardName> Drop: ...")` and returns — `drop()` must never panic, unlike the test body itself. This is the mechanism that makes cleanup fire on BOTH the normal-return AND panic-unwind paths without duplicating teardown logic at every early-return site. `ComponentDropGuard` (`tests/e2e_live.rs`) is the second instance of this idiom, modeled verbatim on `AttachmentDropGuard`; reach for this pattern (not a bespoke teardown function) for any future E2E test that creates a live-Jira fixture needing explicit deletion.
- **E2E suite maintenance:** The nightly `e2e.yml` schedule (`0 6 * * *` UTC) is a **data-retention guard**, not just a latency optimization: free Jira Cloud sites deactivate after ~120 days of inactivity and have only a ~15–60 day reactivation window before permanent data deletion. If the nightly job fails with HTTP 401, the `JR_E2E_API_TOKEN` secret has expired — Atlassian caps API tokens at 1 year. Rotate the token in the CI service account before expiry and update the secret in the `jira-e2e` GitHub Environment. Annual rotation is required. Runbook: `docs/specs/e2e-live-jira-testing.md` §9.
- `tests/e2e_cli_surface_guard.rs` — always-run offline guard validating every `jr` subcommand path and flag referenced in `tests/e2e_live.rs` against `jr --help`; catches assumed-CLI-surface defects (nonexistent subcommands/flags) at CI time without requiring `JR_RUN_E2E` or any network access. Does NOT check JSON output shape or exit-code semantics (use serde types or a live run for that). Implemented as E2E-PG-1 / DRIFT-E2E-1; pairs the mechanical `--help` validator with a parser-consistency check (`test_parser_paths_are_subset_of_surface_table`) that flags new e2e_live.rs invocations not yet registered in the guard's SURFACE table.
- All interactive prompts have non-interactive flag equivalents for AI agent usage
- `--output json` on write operations returns structured data (e.g., `{"key": "FOO-123"}`)
- Run `scripts/check-spec-counts.sh` after any edit to .factory/specs/prd/ BC files,
  nfr-catalog.md, or holdout-scenarios.md. Exits 0 if frontmatter counts match body counts.
  Exits 1 with specific mismatch details if drift is detected (DRIFT-001 mitigation).
- Run `scripts/check-bc-cumulative-counts.sh` after any edit to BC-INDEX.md, CANONICAL-COUNTS.md,
  or `total_bcs:` frontmatter values. Exits 0 if all 8 surfaces agree: per-file frontmatter
  (Surface A), BC-INDEX.md Section headers (B), BC-INDEX.md sections: lines (C),
  CANONICAL-COUNTS.md per-file table (D), body preamble prose, BC-INDEX.md frontmatter
  total_bcs (Surface E), CANONICAL-COUNTS.md Sum row (F), and grand-total prose (G).
  Exits 1 with specific mismatch details if drift is detected (DRIFT-002 mitigation).
- **BC Trace and Source fields must not contain numeric test counts.** The `Trace:` and
  `Source:` fields in `.factory/specs/prd/bc-*.md` BC bodies should describe coverage
  qualitatively (file path + test category) rather than enumerate counts. Counts drift as
  tests are added; qualitative descriptions are stable. Enforced by
  `scripts/check-bc-no-numeric-test-counts.sh` in CI. Convention added by PG-365-1.
- `cargo-mutants` is a binary tool installed via `cargo install cargo-mutants --locked` — do NOT add it to `Cargo.toml` as a dev-dependency. Scope and config live in `.cargo/mutants.toml`; policy lives in `docs/specs/cargo-mutants-policy.md`.
- `scripts/check-bc-citation-symbols.sh` — runs in spec-guard CI job; validates `src/` file and symbol citations in `**Trace**:`/`**Source**:` fields of `.factory/specs/prd/bc-*.md` bodies; exits 1 with `BC-CITE-001` offender list if any citation is stale. `--bc-dir` (designed-to-support) + `--src-root` (self-test only) + `--self-test` flags for offline verification. (DEC-148 Guard 1)
- `scripts/check-cargo-mutants-policy-citations.sh` — runs in spec-guard CI job; validates §Scope function-location bulleted list against `src/`; exits 1 with CI-MUTANTS-CITE-001 offender list if any symbol citation is stale. `--policy-doc` + `--src-root` (self-test only) + `--self-test` flags for offline verification. (DEC-150 Guard 2)
- `tests/mutants_glob_existence.rs` — always-run guard validating every `examine_globs` entry in `.cargo/mutants.toml` resolves to ≥1 real file; fails loudly if a refactor orphans a glob entry. (DEC-150 Guard 3)
- **CLAUDE.md dead-citation CI guard (`tests/claude_md_citations.rs`):** `test_claude_md_citations_resolve_to_real_files` reads CLAUDE.md via `include_str!`, extracts every backtick-quoted file-path citation using `extract_path_citations`, and asserts `Path::exists()` for each. Fails CI with canonical CI-CITE-001 message listing ALL dead paths with 1-based line numbers. `.factory/` paths, glob patterns (`*`, `{`, `}`), symbol-form tokens (`::fn`), bare shorthands (`adf.rs`, `ci.yml`), home-directory paths (`~/.config/…`), and extensionless tokens are auto-excluded by the 5-step pipeline inside `extract_path_citations`. ROOT_FILES members (`Cargo.toml`, `CLAUDE.md`, `build.rs`, `deny.toml`, `CHANGELOG.md`, `README.md`, `rust-toolchain.toml`) ARE checked. No CI YAML change required — rides the existing `test` job. Governed by BC-X.13.001, BC-X.13.002, BC-X.13.003.

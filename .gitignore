# Rust build artifacts
/target

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Worktrees
.trees/
.worktrees/
.claude/worktrees/

# VSDD Factory
.factory/
.reference/

# Fixture trees for spec-count tests contain .factory/ subdirs — allow them
!tests/spec-count-fixtures/*/.factory/
!tests/spec-count-fixtures/*/.factory/**

# Proptest persisted regression seeds (local cache, never commit)
proptest-regressions/

# cargo-mutants artifacts (local mutation test runs) — see docs/specs/cargo-mutants-policy.md
mutants.out/
mutants.out.old/

# Demo evidence (per-story VHS recordings + evidence reports) — local-only, not committed
docs/demo-evidence/
